# DEPLOY 2026-08-04 (pm) — HORIZON v26.515 → v26.554

**Baseline (last packaged):** v26.514 (see `DEPLOY_2026-08-04.md`).
**This bundle:** v26.515 → **v26.554** (current HEAD of `phase-2.1-suppliers`).

> ⚠️ **Live is currently v26.487** (Ben confirmed 2026-08-04) — so the **v26.488→514** bundle is ALSO undeployed. The full outstanding deploy = **v26.488 → v26.547**. Apply migrations **174, 175, 176, 177, 178** (173 optional). See `DEPLOY_2026-08-04.md` for 174/175 detail.
>
> **Live-crash note (Diviyaj, 2026-08-04):** the PO/PAYMENTS 500 was NOT the old build — a supplier typed `3,262.35` (comma) as the invoice value for PO-55AUWK3, and `round(value::numeric,2)` on the pending submission couldn't cast it. Diviyaj already fixed live (bad data + harness hardening + full deploy). **v26.547** ports that hardening into our source (`sanitiseMoney` + `SAFE_NUM_SQL`, both submit paths + approve + the crashing SELECT, portal client sanitise) so it can't recur from Ben's build. **v26.548–550** clear the rest of Diviyaj's backlog: `Array.isArray` guards on Payments Due / portal-actions (can't blank the page on a bad response); portal amount-due now includes `credit_amount` (with a visible line); MANAGE badge uses a single count source (no jump after an edit). **v26.551–554** add the FBA in-flight-transfers feature (migration 179) and the DEMAND plan download (SUG-0016). **This bundle now covers v26.515 → v26.554.**
**Priority reason:** fixes the **live PURCHASE ORDERS / PAYMENTS crash** — `(pos || []).forEach is not a function`. That pattern was refactored out; current code does not contain it. Deploying current HEAD resolves the live breakage.

**New migrations to apply to prod (idempotent — all use IF NOT EXISTS):** `176`, `177`, `178`, `179`.
*(174/175 were in the earlier 08-04 bundle; 173 optional — analytics read-only role.)*
**New env vars:** none. `CIN7_AUTH` (already present for PO push) is also used by the new 3PL Cin7 import — **note: those endpoints read PRODUCTION Cin7.**
**New npm deps:** none (uses existing `exceljs`, `pg`).

---

## Migrations (apply in order, only if not already on prod)
- **179_fba_pending_transfers.sql** — new `planner.fba_pending_transfers` (in-flight Cin7 branch transfers into FBA/AWD not yet in `inbound_shipments`). Powers the FBA-tab in-flight-transfer overlay so a just-processed transfer isn't re-recommended. New endpoints: `POST /api/supply/fba-transfers/refresh` (reads Cin7 /BranchTransfers — **prod Cin7**), `GET /api/supply/fba-transfers/list`. Client overlay only touches the FBA transfer calc (buy plan unaffected). Branch IDs baked in the server (UK FBA 5052 / US FBA 5056 / AU FBA 16289 / US AWD 27816 / EU FBA 10879).
- **176_tpl_cin7_orders.sql** — new `planner.tpl_cin7_orders` (cin7_id PK, reference, customer_order_no, cost_center, member_cost_center, invoice_date, branch_id, total, freight_total, period, imported_at). Upsert cache of imported Cin7 sales orders for 3PL invoice mapping. No dupes (PK = cin7_id).
- **177_tpl_cin7_imports.sql** — new `planner.tpl_cin7_imports` run-log (tpl, period, from_date, to_date, orders, kind, ran_at).
- **178_tpl_cin7_imports_status.sql** — adds `status`, `error`, `cin7_calls` to `tpl_cin7_imports` (powers the Import log tab).

## Headline fix
- **Live crash resolved.** The `(pos||[]).forEach` code path no longer exists. Additionally hardened `renderPaymentsReport` (v26.544) to tolerate a non-array API response (falls back to `[]` instead of throwing), so a bad endpoint degrades gracefully rather than blanking the page.

## Server (`server.mjs`) — new/changed since v26.514
- **3PL Invoicing endpoints** (all read-only except the file upload/delete + the Cin7 cache upsert), under `/api/supply/tpl/`:
  - `POST parse/:id` — workbook parser (extended: fullest-row header detection; AUD; Coghlans column names).
  - `POST map/:id` — map order refs → Cin7 cost centre from the `tpl_cin7_orders` cache.
  - `POST cin7-import` — monthly incremental import by **invoice date**, **branch-filtered** (EU 25073 / US 5055 / UK 5053 / AU 16288), **completed days only**, logged. Passes the `Authorization` header (CIN7_AUTH).
  - `POST cin7-sweep/:id` — reference-based clean-up sweep (any date/branch, 50/chunk, retries transient errors).
  - `GET cin7-log` — import audit log.
  - `POST xero-bill/:id` — Xero bill-import CSV **and** `preview:true` JSON (structured lines). **Region-aware tax:** EU/UK `20% (VAT on Expenses)` (amount blank), AU `GST on Expenses` (amount = line/11), US `Tax Exempt`. Non-order sheet totals + invoice-recap-only lines (`_tplInvoiceExtras`: Support/Storage/Returns/Packaging) + per-cost-centre freight/fulfilment (account from `tpl_account_map`).
- `GET/POST /api/demand-revenue-targets/periods` + `/periods/bulk` — per-subcategory month/quarter/half growth targets (mig 175); template download/import.

## Artifact (`artifact_v16.7.html`) — by area
- **REPORTS ▸ 3PL Invoicing** (`/reports/3pl-invoicing`): full workflow — 4-step stepped layout (1 Upload+files merged · 2 Import Cin7 · 3 Clean-up sweep, conditional/hidden when nothing unmapped · 4 Review + download); 2-column Review (Xero bill preview left / data analysis right); auto-map on Analyse; import log tab; AU Coghlans supported (generalised parser); Download **CSV for Xero Bill Upload** (light blue); fast 120ms tooltips; left-aligned tables.
- **DEMAND ▸ Summary**: Add-targets editor (per-subcategory growth %, half→quarter→month inheritance) + Rev+targets metric; **Summary with targets** pop-out; **Target recommendations** (recommend forecast % changes to hit revenue targets, ASP-aware, apply as hard units); download template + import; filter pills persist.
- **FBA grid**: (a) Transfer FBA (non-GRS) lists only SKUs with an actual transfer; (b) PARTIAL excludes whole-carton SKUs (no full+partial double-send); (c) non-GRS uses pure carton math (no demand floors); (d) override cell shortcuts `2c` / `50r` (same as Order Plan); (e) **ANY** mode carton-rounds whole-carton fillers (shows 560 not 589), stragglers raw. FULL path byte-identical → buy plan unaffected.
- **DEMAND plan download** (SUG-0016): toolbar "⬇ Download plan" → FY-picker → xlsx (SKU × month, Units + Revenue, actuals/forecast flagged), respecting on-screen filters. Extracted `skuMonthlyMap()` shared by the grid + export. No migration.
- **3PL reference standing rules** (server map + xero-bill + sweep): `FBA*` → Amazon fulfilment for the region (EU→`COGS - DE - Amazon`, UK→`COGS - UK - Amazon`, US/AU likewise); `TRF*` → Other costs (branch transfers, the `other` cost-type account). These are excluded from the Cin7 clean-up sweep. Map/sweep surface the first 50 genuinely-unmapped refs + a rule-assignment note.
- **Payments Report** (`supply/inject.html`): tolerate a non-array API response (fixes the class of live `(pos||[]).forEach` crash on redeploy).

## SUPPLY (`supply/inject.html`)
- Payments Report non-array hardening (v26.544).

## Testing notes (sandbox, v26.544)
- 3PL: EU iFulfilment + AU Coghlans both reconcile to invoice (Coghlans excl-GST total 7,910.62; AU GST amounts = line/11). Cin7 import branch-filtered + completed-days; sweep matches cross-month refs.
- FBA: non-GRS Full/Partial split verified against Ben's UK examples; buy plan output unchanged (FBA_CARTON_MODE default FULL; only PARTIAL path changed).
- Both `artifact_v16.7.html` and `supply/inject.html` pass per-`<script>` syntax checks.

## Deploy
- Pull `phase-2.1-suppliers` @ **v26.544**. Apply migrations 176/177/178 (idempotent). No env/dep changes. Restart the harness.
