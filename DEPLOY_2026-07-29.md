# Deploy note for Diviyaj — 2026-07-29

**Live baseline:** v26.243 (confirmed live on Horizon; migration 156 applied & verified).
**This package:** v26.244 → **v26.273**.

Pull `phase-2.1-suppliers` (HEAD = v26.272) into the production repo, wire to prod data, deploy.

---

## Migrations to apply (production Supabase `oolwklahstnvocaugryg`)

Three new migrations since v26.243 (156 is already applied):

- **`migrations/157_note_mentions.sql`** — `supplier_notes.private` + `.mentions` (internal team-only @-mention notes).
- **`migrations/158_po_change_log.sql`** — new `planner.po_change_log` table (PO timeline record-of-change audit log). **Server code is defensive**: without the table, the audit log just renders empty — nothing else breaks — but please apply it so the feature works.
- **`migrations/159_email_log.sql`** — new `planner.email_log` table (CONFIG ▸ Email log). Also defensive: without it, logging is a no-op (never blocks a send) and the panel is empty.

No other schema changes.

## ✅ ORDER PLAN TOTAL-row fix — now committed (stop re-applying)
The PO-detail order-plan `op-total` footer had 7 cells vs the 8-col header (qty landed under "Exceptions"). That's fixed at **v26.244** (the first commit in this package) — the missing Exceptions-column `<td>` is now in the branch and intact on HEAD. **You no longer need to re-apply it each round.** (I verified all ORDER PLAN total/footer rows now balance to their headers.)

## Files changed (v26.243 → v26.272)
- `server.mjs`
- `supply/inject.html`
- `supply/portal-view.js`
- (`CHANGES.md`, `package.json` — version log only)

## New env vars / dependencies
- **None.**

## n8n — already actioned by you (no further action)
- `colour_swatch_url` + `size_long` added to the SKU_CHILD → `planner.products` map (COALESCE-guarded, nightly 02:30). Barcodes pick up swatch + size from tomorrow. 👍

---

## What's in this package (v26.244 → v26.272)

**Payments / Xero**
- Xero Compare tab (period-aware snapshot, 24h persistence, 5% tolerance + variance %, FX-gain neutralisation).
- Xero export: NO-DEPOSIT PO payments derive the account code from the production no. (`620.xx Pxx`; AU → `620.00 AU`).
- New **PAYMENTS ▸ By Supplier** report (invoice total / starting / completion / balance; supplier·prod·batch filters).

**PO timeline / notes**
- Internal @-mention notes (team-only, email the tagged teammate; email preview on post) — **needs migration 157**.
- @-mention picker: keyboard nav (Tab applies) + un-clipped fixed picker; mention email deep-links to the PO timeline.
- **Record-of-change audit log** on the PO timeline (status / prod-end / shipment / deposit / payment edits + ERP uploads) — **needs migration 158**.

**Order Plan / Actions / Reallocate**
- Order Plan: PO header → drawer, +production-status/client/order-ref, top-aligned cells, COMPLETED status pill, "All" pill removed (none-selected = all), TOTAL-row fix.
- Actions: amber payment actions open the PO drawer; "Hide payment noise" pill.
- Reallocate report: correct SKU/PO/supplier mapping, **same-supplier-only** moves, PO refs → drawer.
- Order plan / invoice parse: **Cancel/Reject** supplier changes (new `POST /api/supply/po-line-reject`).
- Filter persistence across PO grid / Shipments / Order Plan (localStorage, sliding ~1h TTL; search boxes excluded).
- PO grid "CSV for Fulfil" → "Export CSV".

**Manufacturing / Scenario / Product**
- Manufacturing tab: persisted shared notes area; PO links → drawer; "Recently closed orders" (last 30 days).
- Scenario ▸ B2B: in-production stock analysis — "Fulfil from" toggle (on-hand+inbound / in-production / mix), per-SKU in-production POs (Direct-to-Client excluded), proposed actions (ship / expedite ≤4wk / make-more-&-expedite).
- Carton/inner barcode labels: carton qty now from `planner.products.carton_qty` (fallback sku_labels).

**Supplier portal**
- Expanded PO: darker top/bottom rules; mobile PO grid first column shrunk to fit the "M" button + badge; mobile production group header shows just "P# NN" (count hidden).

**Admin / CONFIG**
- **CONFIG ▸ Email log** (admin-only): logs every Resend send; sent-per-month counts + date range (default 7 days) + search — **needs migration 159**.
