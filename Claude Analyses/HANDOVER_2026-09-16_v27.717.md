# HORIZON handover — 2026-09-16 evening (v27.717) — for the NEW MACHINE

**Branch:** `phase-2.1-suppliers` · **HEAD:** v27.717 (`3fb758f9`, pushed) · **Live:** v27.694 · **Repo:** https://github.com/dockandbay/demand-planner-ben.git
**Prod Supabase:** `oolwklahstnvocaugryg` (Diviyaj owns writes; read via Supabase MCP) · **Sandbox Supabase:** `mgqrcupazffvpzpeuxzt` (Ben's, via `.env DATABASE_URL`; migrations applied through **280**)

Read this first in a fresh session. Supersedes `HANDOVER_2026-09-16_v27.695.md` (still worth reading for the Storage-uploads detail, section "PRIMARY WORKSTREAM").

---

## 0. New-machine setup (do this before anything else)

1. `git clone https://github.com/dockandbay/demand-planner-ben.git` then `git checkout phase-2.1-suppliers`. GitHub auth: `gh auth switch --user dockandbay` (pushes 403 under the other account).
2. `npm install` (deps incl. `pdfjs-dist`, `jsdom`, `pg`, `exceljs`, `ssh2-sftp-client`).
3. **Copy `.env` from the old machine** (never committed). Keys it holds: `DATABASE_URL` (sandbox, Session pooler port 5432, IPv4), `LIVE_DATABASE_URL` (placeholder), `ANTHROPIC_API_KEY`, `PLANNER_KEY` (blank locally), `CIN7_AUTH` (**LIVE Cin7**, local Cin7 endpoints hit production), `WEBDAV_BASE`/`DRIVEHQ_*`/`TARGET_FOLDER`, `DEV_USER`, `FULFIL_SANDBOX_*`, `BLADE_PASSWORD`, `COGHLANS_SFTP_PASSWORD` (empty, blocked). Still ABSENT and needed: `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (sandbox), `DHL_API_KEY`.
4. **Copy Claude's memory directory** — it lives outside the repo: `~/.claude/projects/-Users-bm-Documents-CLAUDE-horizon-demand-and-supply-planner/memory/`. On the new machine the folder name is derived from the repo path, so clone to the same path (`~/Documents/CLAUDE/horizon-demand-and-supply-planner`) or rename the memory folder to match. `MEMORY.md` is the index; `work-queue.md` is the validated open list; `test-signoff-queue.md` items 96-110 are today's unverified changes.
5. **Copy `mockups/`** (gitignored; holds the approved PRODUCT split mockups `product-split-grids.md`).
6. Start: `node server.mjs` → http://localhost:8124 (restart = `pkill -9 -f "node server.mjs"; nohup node server.mjs > /tmp/hz_server.log &`). Sandbox DB is remote, nothing to apply — migrations 1-280 already there. Optional: `cloudflared` for phone/portal tunnels.
7. Sandbox PRODUCT fixture (wipe + reseed, sandbox only): `node scripts/dev/seed_product_fixture.cjs` → SS27-TOWEL-CLASSICBLUE (BL/XR/MQ requests), SS27-HAIRW-LOVELYHAIR (LX), CUST-COOLI-NEXTBROWN (BE). Live PRODUCT dev tables are EMPTY, so faking sandbox data is approved.
8. jsdom render probes: `scripts/dev/render_check.cjs <served html>` (stubbed fetch) or write a probe with real `fetch` against :8124 (pattern in this session's scratchpad probes; the recipe is in memory `verify-client-render-not-just-endpoints.md`). No headless Chrome.

---

## 1. What shipped today (16-Sep) — v27.696 → v27.717, all pushed

| Ver | What | Server / mig |
|---|---|---|
| 696 | EU iFulfilment 3PL invoice: stop double-counting Consumables (Aug-26 = 46,190.59 = invoice) | server |
| 697 | PO order-plan "⤓ Download barcodes ▾"; portal production barcodes get batch + unsquashed title (builders unified) | server |
| 698 | Category colours (`categories.colour_hex`, palette auto-assign, Config picker, badges everywhere) | **mig 277** |
| 699-701 | PRODUCT headers flush left; rich compose `hzRichCompose` (Pantone chips inline) on timeline + samples + batch review | client |
| 702-703 | **PRODUCT split P1**: development requests model (`product_dev_requests`, `_request_components`, samples.request_id/received_at, derived product stage + override), grid on requests, Requests tab, request form | **mig 278** |
| 704 | **P2 SAMPLING page** (`GET /api/product/sampling`, received ticks, inline per-sample tabs, Age, filters) | server |
| 705 | Portal Productions: Total column + TOTAL footer (+XLSX) | client |
| 706 | Timeline change-record bursts merged (`hzGroupChanges`) | client |
| 707 | Invoice parse: duplicate SKU lines summed + warnings (portal, email, admin); NEW portal routes `/api/portal/parse-invoice`, `/api/portal/invoice-apply` | server |
| 708 | **P3 Documents**: versions/by/Latest radio, client thumbnails (pdf.js served from `/vendor/pdfjs`), swatch inherits latest thumb | **mig 279** |
| 709-711 | PLAN → PRODUCTS (`#/product/products`), swatch enlarge cache-bust | client |
| 712 | Product detail polish; one-button "+ Add component" picker; component "A" actions only while a request is Sample in review | server |
| 713 | **Sampled vs spec-linked PER SIZE × COMPONENT**; link / upload spec from the grid; "Sample requests" column; no supplier on components; `comp:<id>` dims accepted; deep-link tab fix | **mig 280** |
| 714 | SAMPLING layout: batch review as a view of SAMPLING (L2 tab removed), swatch S/M/L + hover-enlarge, Colour/Type column, wider Stage, slim ticks | client |
| 715-716 | Colour / Type = colour way + category badge; type badge under supplier; Stage + Age merged | client |
| 717 | Sample tabs descend from their tick columns; deep link `#/product/sampling/<request ref>/S<n>`; URL follows open/switch/collapse | client |

Also produced: corrected Xero bill + journal CSVs for EU iFulfilment Aug-26 in `~/Downloads` (xero-bill-EU-iFulfilment-2026-08.csv = 46,190.59; journal-EU-iFulfilment-2026-08.csv, Sep order EU-49290 5.00). Copy them if still needed.

---

## 2. Pending for Diviyaj (two packages)

1. **v27.695** — `deploy notes/DEPLOY_2026-09-16_v27.695_storage-uploads.md`: mig 276 + private bucket `horizon-uploads` (100MB) + prod env `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`. **HOLD** until the sandbox >4MB round-trip test passes (needs Ben's sandbox service-role key).
2. **v27.696 → v27.717** — `deploy notes/DEPLOY_2026-09-16_v27.696-697.md` (title extended). Migrations **277, 278, 279, 280** (all additive; PRODUCT tables empty on live). New portal routes (707). `/vendor/pdfjs` static route from `node_modules/pdfjs-dist` — verify it resolves on Vercel. No new env vars or deps. Independent of package 1.

Live = v27.694 (confirmed via /api/version 16-Sep). Live schema: migs 270-275 present, 276+ absent.

---

## 3. PRODUCT split — where it stands

Scope: `Claude Analyses/SCOPE_PRODUCT_SPLIT_2026-09-16.md` (all 11 decisions recorded, DHL section 7, phase table). Mockups A-D approved.

**Model (greenfield, live empty):** `product_dev_items` (ref `SEASON-TYPE-COLOURWAY`, editable; `stage` NULL = derived from requests) → `product_dev_components` (catalogue-typed; `sampling_mode` = default for new sizes; `supplier` column kept only for portal ownership until P5) → `product_dev_requests` (one supplier × components × sizes; own stage/approval/countries/dev_start/stakeholders/notify; ref `<product>-<supplier code>`) → `product_dev_samples.request_id` (versions per request, `received_at`). Per size × component state in `product_dev_size_dimensions` (dimension = legacy name or `comp:<id>`): required, approval, description, files, **sampling_mode/spec_id (mig 280)**.

**Done:** P0 rich compose · P1 requests model + grid · P2 SAMPLING page · P3 documents/thumbnails · today's 712-717 polish batch.

**Remaining, in order:**
- **P2b DHL Unified Tracking** (KEY requirement): CONFIG ▸ Admin panel + env `DHL_API_KEY`, server-side poller → cache table `carrier_tracking` (planned **mig 281**), pills on samples (`sample_requests.tracking_code[_2]`) and bulk shipments (`shipments.carrier_ref` → `tracked_delivery_date`, `tracked_source='dhl'`), n8n schedule. Build against a stub; live test needs Ben's key.
- **P4** QR / short code on the sample card + phone scan feedback page (auto-receive OFF by default).
- **P5** portal re-scope to requests (supplier sees only their requests; nothing until a request exists) + DROP moved item columns (`supplier`, `supplier_code`, `recipient_countries`, `approval_method`, `dev_start_override`) and `product_dev_components.supplier`.
- **P6** timeline `/s` chips: tag the actual sample version ("Sample N (BL v2)"), note stored with sample_id (+component_id) = feedback (planned **mig 282**); retire hard-coded "sample 1/2" tags.
- Open UI question from Ben's brief: sampling-grid item 2(d).

**Key code (supply/inject.html):** `renderProductSampling` / `smpExpand` / `smpHash` / `_smpDeep` (~13260-13380); `renderProductBatchReview`; `renderProdComponents` (compact chooser, `#pc2-addpop`); `renderProdMaster` sizes tab: `compRow`, `compMode`, `compModeSel`, `compReqCell`, `bindSz` handlers `.pm-comp-mode/.pm-comp-speclink/.pm-comp-specup/.pm-comp-specunlink/.pm-req-add`; `openSpecUploadPop`; `openRequestForm(ref,{component_ids,after})`; `prodRequestChip`; `hzHoverZoom`; `_prodOpenTab` (deep-link tab); router `seg[0]==='product'` (~10760). **server.mjs:** `REQS_JSON_SQL`, `SD_NOT_SPEC_SQL`, `applyDerivedStage`, `isProdDim`, `/api/product/size/:id/dimension`, `/api/product/sampling`, `/api/product/sample/:id/received`, `/api/product/specs` (POST; scope 'sku' used for grid uploads), `/api/product/component-type`.

---

## 4. Everything else still open (validated list = memory `work-queue.md`)

**Blocked on Ben:** sandbox `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` (>4MB test) · Coghlans SFTP password + proxy port/auth · DHL API key · test sign-off pass (items 96-110 today; ~150 older) · Manufacturing tab definitions · BALLAST-SUM-CSTCANDY UK 720 buy (↻ live cache) · Coghlans GST 10% confirm + Sep re-run · MIXNMATCH / SS27 undefined sets + trailing-space SKUs (Airtable) · products.moq all null on live.

**Waiting on Diviyaj:** the two packages above · signed-cookie auth mirror · n8n Cin7 3PL-orders flow (then remove in-app import) · `sku_labels` DROP · legacy Vercel cron removal · polybags n8n wiring · live portal `hz-theme.css` link check · stale PR #2.

**Ready to build (no decision needed):** remove `EXCLUDED_INBOUND_REFS` hard-code (snapshot buy before/after) · portal preview sub-tab deep links · 3PL Forecast auto-forecast inbound column · sets demand-plan hover follow-ups · PRODUCT redesign #10/#14/#16 · portal last native confirm → sheet · **queue item 42: portal Productions PO approval status / Approve all / "(2) of (6)" header** (reuse existing confirm flow).

**Bigger builds awaiting a go:** Buy v2 grouped rows (slices 3+) · SS27 sets-for-Core too high investigation · B2B run-off launch-spike rule in engine · auto-smooth post-discontinue · 3PL forecast weekly export · sandbox-refresh-from-live admin tool · Coghlans reclass automation · mobile M5 · backlog specs list in MEMORY.md.

---

## 5. Standing rules that were confirmed today (also in memory)

- Push to GitHub after every change without asking (Ben, 16-Sep). Commit and push as separate commands; never amend a pushed commit.
- Sandbox PRODUCT data is disposable: fake, wipe, reseed freely (live tables empty).
- Version bump (package.json + CHANGES.md) on every change; extend the current deploy note rather than starting a new one until Diviyaj pulls.
- Verify with jsdom RENDER probes against the sandbox, not just endpoint 200s; kill probes when done.
- Dates dd-mmm-yy in UI; grids left-aligned; no visible reload on auto-update; never trust proxy headers for auth.
- Commit trailer: `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>` + `Claude-Session: https://claude.ai/code/session_01XVBaV7F8P9v2Zkc4m2QA2Y`.

---

## 6. First actions in the next session

1. Confirm `.env`, memory folder and `mockups/` copied; `node server.mjs`; open http://localhost:8124/#/product/sampling and http://localhost:8124/#/product/SS27-HAIRW-LOVELYHAIR/sizes to eyeball today's work (sign-off items 107-110).
2. If Ben gives the DHL key → start P2b; otherwise build P2b against a stub, or take queue item 42 if Ben prefers.
3. When Diviyaj is ready, hand over the two deploy notes above.
