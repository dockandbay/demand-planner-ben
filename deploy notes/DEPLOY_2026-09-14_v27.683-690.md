# Deploy note — v27.683 → v27.690 (for Diviyaj)

**Date:** 2026-09-14 (session 2)
**Branch:** `phase-2.1-suppliers` (pushed, HEAD 9b2b2d93)
**Prev package:** v27.677–682 (`DEPLOY_2026-09-14_v27.677-682.md`)

---

## Migrations to run on live
- **`273_sample_request_recipients.sql`** — adds `recipients jsonb NOT NULL DEFAULT '[]'` to `planner.sample_requests` (multiple free-text recipients per sample). Additive, safe. (Applied to sandbox.)
- **`272_key_account_client_fba_fields.sql`** — from the v27.677–682 package; **run it if you haven't already** (it's a prerequisite for v27.679's key-account Client/FBA fields).

No new env vars. No new npm deps.

---

## ⚠ Buy-affecting (verify buy plan)
- **v27.686 — FBA transfer rework.** Four changes to the 3PL→FBA transfer engine (`artifact_v16.7.html`): (D) inbound now lands by its ETA date — inbound arriving later in the current month is credited to this month, not clamped to next (forward-month bucketing is for buys, not dated inbound); (C) `fbaTransferRec` counts near-term (this-month) 3PL inbound in the transferable pool; (B) the dual-channel website hold-back now bridges only to the next substantial 3PL replenishment (≤4wk) instead of a flat 4 weeks; (A) an empty-FBA SKU under one carton is seeded with ~1 month of FBA demand instead of 0 (a full carton still wins when affordable). **Buy delta on our sandbox = 3 SKUs only** (bp_snap before/after: UK −60, EU +30, AU +50); fixes e.g. HAIRW-SUE-JNGLBLM UK (was 0 transfer). Worth a spot-check of the FBA tab + a buy-plan diff on live.
- **v27.683 — buy popup current-month "DTC demand (sets)" nets set actuals-to-date.** Buy **byte-identical** on our data (only trims the current partial month where a set has sold MTD).

## Not buy-affecting (PRODUCT + UI)
- **v27.684** — PRODUCT grid **Supplier filter** (only suppliers with products in the grid); **Pantone chips show the colour name** after the code; timeline compose box `autocorrect`.
- **v27.685** — timeline **tags: "+ new tag"** (creates + selects) and a **× to remove** a tag from a message; **compacted the PRODUCT grid filter bar**.
- **v27.687** — sample **received date is editable** (`/api/supply/sample/:id/received` accepts an optional `date`); the most-recent **sample card is collapsible**.
- **v27.688** — **sample batch review N+1 fix** (`/api/product/batch-review/:id`: parallelised per-product lookups, `productSampleList({skipFeedbackNotes})` drops an unused `supplier_notes` LIKE scan — ~2.1s→1.3s on sandbox, much less DB CPU); **product swatch** in the batch-review Product cell.
- **v27.689** — batch review reorganised into **collapsible product groups** + **Minimise all / Expand all**; a product's off-batch sample versions show, hidden behind **"show N past samples not on this batch"** (server flags `on_batch:false`).
- **v27.690** — sample requests take **multiple free-text recipients** (mig 273).

Files: `server.mjs`, `supply/inject.html`, `artifact_v16.7.html`, `migrations/273_…sql`.

## FYI
- `/api/supply/sample/:id/received` now accepts `{date}` (optional); `sample-create` / `sample-detail` / `sample/:id` handle the new `recipients` jsonb.
- No data-only live changes in this session beyond what's above.
