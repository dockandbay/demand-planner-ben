# Deploy note — 2026-07-31 (for Diviyaj)

**Live baseline:** v26.299 (your merge: v26.273→299 on the hardened build, migrations 159/160/161 applied, verified).
**This package:** v26.300 → **v26.345**.

Ported cleanly on your side last time — thanks. This bundle is Ben's product changes since then. Full detail per version in `CHANGES.md`; summary + the bits you need to wire below.

---

## ⚠️ Migrations to apply (2)
Both are `IF NOT EXISTS`, additive, safe to re-run:

- **`migrations/162_suggestion_stakeholders.sql`** — adds `planner.suggestions.stakeholders text`. Needed by v26.302 (per-suggestion stakeholder emails, SUG-0003). *Not yet on live — it's above your v26.299 cut.*
- **`migrations/163_branch_fulfil_id.sql`** — adds `planner.branches.fulfil_id text` (Fulfil warehouse mapping). **Schema only** — do NOT seed id values in the migration; the ids are environment-specific and are entered per-env in CONFIG ▸ Branches. (Sandbox was seeded separately by Ben.)

No new npm dependencies.

---

## New env vars (OPTIONAL — Fulfil integration, gated off by default)
Only needed if/when we start testing the Fulfil ERP path. **Nothing breaks without them** — with no keys, every Fulfil op throws a caught "not configured" and nothing sends.

```
FULFIL_SANDBOX_SUBDOMAIN=
FULFIL_SANDBOX_API_KEY=
# later, for live Fulfil:
FULFIL_LIVE_SUBDOMAIN=
FULFIL_LIVE_API_KEY=
```
See the Fulfil section below for the safety gates.

---

## What's in this bundle (grouped)

### 1. Suggestion box + supply/portal fixes (v26.300–321)
- Email log: wider Subject column, "Sent per month" client-side fallback (v26.300–301).
- **SUG-0003** per-suggestion stakeholders (**needs mig 162**); SUG-0011 supplier-named emails; **SUG-0012** PO-grid SKU filter; **SUG-0013** "Likely pay" date → Cash Flow (v26.302–305).
- PO grid: removed "N/N POs" count; "Shipped · no final invoice" exception filter; shipment-member indent only when master in view (v26.306, 311, 312).
- Emails: Reply-To = the person who triggered them; new-suggestion subject names the submitter (v26.307–308).
- Deposits: supplier filter + remembered filters; closed deposits greyed in the picker; portal deposits expand to POs that drew them down (v26.309, 310, 316).
- Payments Due: completion-owed shows; no false "$0 balance overdue"; By-Supplier "Due" column; balance milestone has no due date when calc balance = $0 (v26.313–315).
- Suppliers: fixed add failing on kind-check constraint; inline add form (no double prompt) + duplicate-code handling + bigger button; also fixed portal confirm/withdraw buttons never showing (missing query columns) (v26.317–319, 321).
- PO timeline: supplier + internal note composers side by side (v26.320).

### 2. Fulfil ERP integration — GATED, safe to ship (v26.322–336, 338)
Mirrors the existing "Update ERP" (Cin7) process; targets Fulfil only when explicitly switched. **All of the following gates are in place:**
- **Active ERP defaults to `cin7`** (app_settings `erp_integration`). Toggle in CONFIG ▸ General settings.
- **`FULFIL_LINES_SEND = false`** (hard-coded const in `server.mjs`) — the create/line push runs read-only *resolution only* and returns the payload it *would* send (dry-run). Flip to `true` only after validating against the sandbox catalog.
- With no env keys → throws caught `NO_FULFIL_CFG`, nothing sends.
- Fulfil API confirmed: base `https://<subdomain>.fulfil.io/api/v2`, auth header `X-API-KEY`, PO model `purchase.purchase`.
- Supplier→party via `planner.suppliers.fulfil_id`; branch→warehouse via `planner.branches.fulfil_id` (mig 163); currency via `suppliers.default_currency`. Editor fields + hints added in CONFIG.
- **Blocker before real use:** the Fulfil sandbox needs its product catalog + suppliers imported (currently 0 products) — that's the ops task to enable an end-to-end test. Not required to deploy this code.

### 3. Demand plan visual overhaul (v26.325–345, artifact only)
Colour/layout cleanup: actuals green text (#F0FFF0 cells), discontinued cells pink (#FFD6D1), removed blue anomaly tint, unified FY columns (#e7e5e0), uniform black forecast numbers; category header cleanup (redundant single-subcategory headers dropped, real ones pin their name; dark 2px category dividers + darker subcategory dividers); tighter data-cell padding; col-1 subcategory names wrap; larger SKU-row numbers; hid the redundant "N SKUs" row; fixed the -100%/missing-pink bug on discontinued stock-out SKU cells. All in `artifact_v16.7.html`.

---

## SUG-0007 — acknowledged, nothing owed from Ben's side
Understood: Part A (oauth2-proxy logout) doesn't apply — HORIZON does Google login in-app via Supabase. The app-side 7-day guard shipped in v26.288 (already on live). Reloads instead of hard-logout until you wire the Supabase session length + in-app logout route. No action needed from Ben.

## Files changed
`server.mjs`, `supply/inject.html`, `artifact_v16.7.html`, `migrations/162_suggestion_stakeholders.sql`, `migrations/163_branch_fulfil_id.sql`.
