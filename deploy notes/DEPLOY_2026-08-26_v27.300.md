# DEPLOY 2026-08-26 (#2) — HORIZON (for Diviyaj)

**Package:** LIVE is **v27.297** → deploy to **v27.310** · Branch: `phase-2.1-suppliers` · From: Ben

**NO migrations · NO new env vars · NO new npm deps.** Pure code (server.mjs, artifact_v16.7.html, supply/inject.html, package.json, CHANGES.md). Standard pull + Vercel deploy. (Follow-up to the v27.275→v27.297 package deployed earlier today; migration 244 + the pdf-lib/jszip/pdfjs-dist deps are already live.)

---

## What's in this package (v27.298 → v27.310)

### EDI Labels — made serverless-safe (your flag)
- **v27.298** — dropped the in-memory job cache. **Parse and generate are now stateless**: generate re-parses from the files re-sent in the same request (client bundles `csv_base64`+`pdfs`+`choices`+`preorderRefs`), sharing one `_ediParse` helper. No jobId, no cross-request state → survives Vercel. Verified byte-for-byte: same 1,316-file ZIP as before. (This resolves the "won't survive on Vercel" note.)

### Auto Forecast — forward-only (Ben)
- **v27.299 / v27.300** — the Auto Forecast cash view was posting **near-term duty / balance / completion for orders it can't actually place** (a September UK landing needs a ~May order — already past). Both engines now **skip any order whose placement month is already past** and surface the units as an **overdue count**; the first projected landing is now + lead (UK ≈ December).
  - v27.299 fixed the legacy **Rolling** engine; v27.300 fixed the **default Buy-plan engine** (Option B) too (it had been rolling overdue orders to "order now" but keeping their original arrival) — and a rolled current-month order's arrival now = placement + lead.
  - Both `GET`+`POST /api/scenario/auto-forecast` now send **`Cache-Control: no-store`**.
- **⚠ Behaviour change (report-only, no buy-plan/DB writes):** near-term Auto-Forecast cash **drops** — e.g. sandbox September import duty fell from £1,123 to £237, balance £5,358→£1,460, completion £11→£0; **deposits unchanged** (order-month leg = genuinely forward orders). Live September import duty (£104,422) will drop similarly and the duty curve ramps from ~December. Users should **reload once** (a per-session client `AF_CACHE` clears on reload).

---

### Auto Forecast — drill-down (v27.301)
- Every cell in the **Payments plan — cash out by month** table (USD + GBP) is now **clickable** → popup of the order legs behind it (reference `FC-<market>-<order-month>-<supplier>`, country, supplier, GBP, total). Reads the report's own transactions; report-only.

### PO grid ▸ Client/FBA — delete attachments (v27.302)
- Each uploaded Client/FBA attachment now has a **✕ delete** button (was download-only), via existing `/api/supply/po-doc-delete`. Removes the row from `planner.portal_attachments`.

### EDI Labels ▸ Step 7b — Cin7 sales-order upload (v27.303)
- New **Step 7b** builds a **Cin7 sales-order upload CSV** from the **ASN CSV only** (no PDFs). Two buttons:
  - **By PO (file 4):** one order per PO (`Order Ref = DILLARDS-<po>`), SKUs aggregated across stores, qty summed, item price blank, delivery instructions "Crossdock to Geneva then dispatch".
  - **By store (file 3):** one order per PO × store (`Order Ref = DILLARDS-<po>-<store>`), ship-to = the store DC address from the ASN, **item price = `products.us_rt / 2`**, billing = DILLARDS / keyaccounts@dockandbay.com, delivery instructions "Dillards Special Requirements to be provided".
  - EAN → SKU + `us_rt` via `planner.products`; **qty summed from ASN, qty-0 SKU omitted** from that order; rows sorted PO → store → SKU.
- Server: new stateless `POST /api/supply/edi-labels/sales-orders` (re-parses re-sent CSV; serverless-safe). Write-gate exemption widened to all `/api/supply/edi-labels/*`. **No migration / dep / env changes.**
- **⚠ Price to confirm:** the sample file 3 Ben supplied showed item prices of 8.5 / 17 / 19.13 (≈ `us_rt × 0.425`), whereas Ben's instruction was **`us_rt / 2`** (10 / 20 / 22.5). Built to `us_rt / 2` per instruction — flag if the halved price needs revisiting.
- **v27.304** — Step 7b button labels renamed to **Sales Order by PO** / **Sales Order by Store** (label-only).

### DEMAND ▸ ASP for future months → seasonal (v27.305)
- Projected-month **ASP** now bases on the **same calendar month last year** (× recorded price-change uplifts) instead of the most recent actual month. Stops a recent discount/sale month from dragging every future month's ASP (and therefore Summary/Targets/plan revenue) down. Fallback to most-recent-actual only when a subcat has no prior-year same-month history. One central `getASP` change; actual/past months unchanged.
- **Verified buy-plan output byte-identical** before/after (sandbox 104,747u total), i.e. no £-target unit shift. Report/revenue-only impact. No migration / dep / env changes.

### BUY PLAN ▸ Key-Account + Preorder demand now reach the buy plan (v27.306)
- **Regression fix:** KA forecasts (`planner.key_account_forecasts`, edited in DEMAND ▸ Inputs ▸ Key accounts) and Preorders (`planner.preorders`) were never folded into buy-plan B2B demand — the loader `refreshBuyData()` was orphaned (no callers) so `md.pka` stayed empty. Now `/api/preorders-ka` is fetched at startup, cached, re-attached in `buildLiveBpOverlay`, folded into B2B by `buildLiveDemand`, and re-pulled by the "Refresh cache" button. **Client-only change** (artifact_v16.7.html). No migration / dep / env.
- **⚠ Buy-plan increases** wherever KA/preorder demand lands in-horizon (intended). Verified: BAGDRY-XS-BLUSKY UK buy 0 → 100 (matches the John Lewis Jun-27 100u KA row). Sandbox global 105,497 → 105,617 on stale data; live swing will be larger.

### Silent auto-update ▸ no idle reload (v27.307)
- A visible tab was reloading itself on idle: the auto-updater reloaded a visible tab after 90s idle when `pendingCode` was set, and a **30-min hard staleness cap force-set `pendingCode` even with no deploy**. Now a **visible tab never force-reloads**; a real deploy applies on the **next navigation** or when the **tab is backgrounded** (silent). Removed the 30-min cap; `/api/version` poll unchanged. **Client-only** (supply/inject.html). No migration / dep / env.

### Smoothing ▸ record-of-change fan-out fix — EMAXCONN (v27.308) — your flag
- Root-caused the `POST /api/forecast/changes/bulk` pooler exhaustion: `applySmoothAlloc` sent a bare un-awaited `logChangesBulk` fetch **inside** the auto-smooth sweep (subcats × months) and the FY smooth — up to hundreds of parallel POSTs per mass smooth.
- **Fix (source, matches your live rehost patch + extends it):** `applySmoothAlloc` now **returns** its rows; all three bulk callers (auto-smooth sweep, FY smooth, single-month) **aggregate → one `logChangesBulk`**. `logChangesBulk` routes through **`postChangesBatched`** (serialised, chunked, retried, `_savePausedUntil` circuit-breaker) and **re-buffers on pause/failure** so the record-of-change is never dropped. I also covered the **auto-smooth sweep** (the biggest fan-out — likely the 5,270-row event), which the manual-smooth-only description may not have.
- **⚠ Small server change:** `/api/forecast/changes/bulk` now accepts `{changes}` as an alias for `{rows}` (postChangesBatched posts under `changes`). Backward-compatible. **Still no migration / dep / env.**
- Verified via harness: 5,270-row smooth → 2 serialised chunks (5000+270), none lost; paused → buffers, no send.

### Targets ▸ recommendations ▸ Apply-all review modal (v27.309)
- "Apply all open" now also at the **top** of Summary ▸ Targets ▸ recommendations; both buttons open a **modal listing every change** (subcat, Forecast £ → Target £, gap, Δ units, total) before writing overrides. Replaces the one-line confirm. Client-only (artifact_v16.7.html). No migration / dep / env.

### EDI Labels ▸ PREPACK pricing + oversize-upload error (v27.310)
- **PREPACK price file:** Step 7b optionally accepts the **PREPACK** doc (EZCOM PO-detail CSV, `UnitPrice` by `UPCCode`); the **by-store** item price then comes from it (matched on UPC), else `us_rt/2`. Server takes `prepack_base64`; header `X-Prepack-Priced` = N matched. (Confirmed with Ben: keep `us_rt/2` as the fallback default.) **POST PACK** = ASN file, **PREPACK** = price file.
- **⚠ User-reported error fixed:** *"Unexpected token 'R', 'Request En'… is not valid JSON"* on Parse & match = a **Vercel ~4.5 MB request-body cap** 413 (base64 PDFs exceed it) that the client mis-parsed as JSON. Client now shows a clear "upload too large — split into smaller batches" message (parse / generate / sales-orders). **This is a UX fix, not a capacity fix** — large label sets still need the real solution: **direct-to-storage upload (Supabase signed URL) or a batched/chunked parse**. That's the same limitation flagged on the `express.json` 25 MB note (server.mjs). Flagging for your side re: whether to wire signed-URL uploads; I can build a client-side batched parse if we prefer to keep it self-contained.
- Client-only + small server-additive (`prepack_base64`). No migration / dep / env.

## Still outstanding on your side (from the last handoff)
- **Phantom PO data fix:** `PO-56UKXR2A` (29 rows / 10,710u into `uk_3pl`) inbound rows never marked received. Once you `UPDATE ... SET received_quantity=quantity` (or delete them), tell Ben and the `EXCLUDED_INBOUND_REFS` hard-code can be removed in a follow-up. **Kept in place until you confirm the data is cleaned.**

## Deploy summary
Pull `phase-2.1-suppliers` @ **v27.310** → deploy on Vercel. No migration / env / dependency changes. Advance baseline to v27.310 when confirmed.
