# DEPLOY — v26.001 (from live baseline v25.569)

Live is **v25.569** (Diviyaj, verified, aliased to horizon.dockandbay.com). This package brings it to **v26.001**.
It's a big batch — the config-nav rework, report upgrades, config-editor rebuild, the move off Airtable MCP,
the whole supplier-portal fix run, a refactor pass, perf work, and a buy-plan data-source correction.

Verified against live DB: migrations **up to 119 are already applied**; **120–123 are NOT** and must be run.

**Version scheme change (heads-up):** from v26 the display version is **zero-padded 3-digit minor** —
`package.json 26.1.0 → "v26.001"` (v25 unchanged). `APP_VERSION` derive updated in `server.mjs`. The
`/api/version` gate string is now `v26.001`; open tabs on v25.569 will still see the mismatch → auto-update nudge.

---

## 1. ⚠️ Migrations — run on live IN ORDER
| # | File | What |
|---|---|---|
| 120 | `migrations/120_supplier_grs_fulfil.sql` | `suppliers.fulfil_id` (Fulfil ERP id) |
| 121 | `migrations/121_export_port.sql` | `suppliers.export_port` + `shipments.export_port` |
| 122 | `migrations/122_weather_cache.sql` | `planner.weather_cache` table (weather moved off Airtable) |
| 123 | `migrations/123_v_po_finance.sql` | **`planner.v_po_finance` view** — shared PO-finance calc. **App 500s on purchase-orders/cashflow/portal until this exists.** |

(Detail for 122/123 also in `DEPLOY_2026-07-19_weather-supabase.md` and `DEPLOY_2026-07-19_po-finance-view.md`.)

## 2. ⚠️ CRITICAL — the artifact now ships EMPTY data placeholders
`artifact_v16.7.html` is 5.07 MB → **0.77 MB**: the big baked data globals (`_SKU_RAW`, `DATA`, `FC_OUTPUTS`,
`PROD_CONST`, `BP_DATA`, …) are stubbed to `{}`/small. **The server MUST inject live data over them at serve
time** (`replaceGlobal` in `server.mjs`, lines ~540). This is in the `server.mjs` we're shipping — but if the
production harness diverges from this repo's inject step, the DEMAND app will load with empty data. **Verify the
served HTML has populated `_SKU_RAW`/`DATA` after deploy** (they should be MBs, not `{}`).

## 3. Files changed (all in this repo)
- **`server.mjs`** (harness — Diviyaj's area, review): shared PO-finance view rewire; **buy-plan `buildPROD_CONST`
  now reads cover/FBA-min/supplier from `planner.products`** (see §5 verify); `/api/weather` + `/api/preorders-ka`
  endpoints (Airtable MCP removed); supplier tax/ERP fields; **gzip the main HTML** (6.3→1 MB wire); **20 s built-data
  cache** (repeat load ~1050→150 ms, invalidated on forecast saves); GBP rate from `app_settings`.
- **`artifact_v16.7.html`**: data stubs (see §2); Auto-Forecast report (GBP CSV + transactions); weather/preorder
  rewired to the new endpoints.
- **`supply/inject.html`**: config nav (2-tier + Supply-chain group); config editors rebuilt (`cardEditor`), showSub
  dispatch map; What's Next popout drawers; PO-detail tab reorg; supplier tax-invoice + export-port + Cin7/Fulfil fields.
- **`supply/portal-view.js`, `supply/portal.html`**: portal Est-cost fallback, Amount-due = value−paid, mobile
  layout (scroll/indent/timeline/confirm-box), expanded-row highlight, version tag in header.
- **`package.json`**: `26.1.0` (displays **v26.001** — new zero-padded scheme). **Dep already present: `exceljs`**
  (invoice generator) — ensure `npm install` on deploy.

## 4. External / n8n (not code)
- **Weather**: repoint the Open-Meteo refresh (was GAS→Airtable `weather_cache`) to write `planner.weather_cache`. Until then the DEMAND ▸ Weather panel shows "no data".
- **Preorders / key-account forecasts**: enact the Airtable→Supabase n8n flow (`planner.preorders`, `planner.key_account_forecasts`) — field maps in `DEPLOY_2026-07-19_weather-supabase.md`.
- **MQ Print (Sherry)**: supplier-name consolidation is **done in the DB** (live + sandbox). Still to do: rename in **Cin7 + Fulfil** so the ERP feed matches.
- No new env vars.

## 5. Verify after deploy
- `/api/version` → `v26.0`; root 200.
- **Served HTML carries live data** (`_SKU_RAW`/`DATA` populated — see §2).
- **Buy plan** renders; spot-check quantities — cover/FBA-min now come from `products.target_cover_weeks_*` /
  `fba_transfer_min_units_*` (verified 0 change for categorised SKUs; **fixes ~101 null-category SKUs** e.g. DOGTWL FBA cover 12).
- **Supplier portal** payment figures match admin (est cost, amount due); mobile layout OK.
- PO grid / cashflow load (they read `v_po_finance`).

## 6. Noted from Diviyaj (parked, non-blocking)
- ~4.5 MB Vercel body cap → direct-to-storage fix (whenever).
- `flexport_shipments.updated_at` migration (Ben offered it).
- `vercel.json` memory setting warning (ignored under Active CPU billing) — safe to strip next tree touch.
