# DEPLOY 2026-08-04 — HORIZON v26.488 → v26.514

**Baseline (currently live):** v26.487 (see `DEPLOY_2026-08-03_v26.487.md`).
**This bundle:** v26.488 → **v26.514**.
**New migrations to apply to prod:** `174`, `175` (migrations 170–172 already shipped on 08-03).
**Optional infra migration:** `173` (read-only analytics DB role — only if setting up the 4 read-only users; see `SETUP_analytics_readonly_access.md`).
**New env vars:** none. **New npm deps:** none (uses existing `exceljs`, `pg`).

---

## Migrations (apply in order)
- **174_revenue_quarterly_targets.sql** — adds `target_q1_pct … target_q4_pct` (numeric, nullable) to `planner.demand_revenue_targets`; `target_type` gains value `'quarterly'`. Backward-compatible.
- **175_revenue_target_periods.sql** — new table `planner.demand_revenue_target_periods` (country, channel, fy, subcategory, level ['half'|'quarter'|'month'], idx, growth_pct). `subcategory=''` = market total. Powers Summary per-subcategory targets.
- **173_analytics_readonly_role.sql** *(optional)* — creates a SELECT-only `horizon_readonly` Postgres role for the conversational-analytics connector. Set the password out-of-band; do not commit it.

## Server (`server.mjs`) changes
- `GET/POST /api/demand-revenue-targets` — handle `target_type='quarterly'` + the four quarter pct columns; GET falls back to the base shape if 174 not yet applied.
- `GET/POST /api/demand-revenue-targets/periods` — per-subcategory month/quarter/half growth targets (mig 175); empty-safe until table exists.
- `POST /api/supply/tpl/parse/:id` — **new** read-only 3PL invoice parser (exceljs; currency-normalised; sums every numeric field per sheet; **excludes embedded totals/summary rows** — a data row must have a non-empty, non-"Total" first column).

## Artifact (`artifact_v16.7.html`) changes — by area
**DEMAND ▸ Inputs ▸ Revenue** (505/506): quarterly growth-% targets with live £ recalc; quarter-grouped LY / Target / Forecast / Δ.
**DEMAND ▸ Inputs nav** (501/510): light-blue level-3 nav + `#/demand/inputs/<slug>` deep links; fixed the sub-menu leaking onto non-DEMAND views.
**DEMAND ▸ Summary** (508/513): Units/Revenue + Month/Quarter/Half toggles; **Add targets** editor (per-subcategory growth % with half→quarter→month inheritance) + **Rev + targets** metric (forecast vs target + variance).
**REPORTS ▸ 3PL Invoicing** (507/511/512/514): slug renamed `/reports/tpl` → `/reports/3pl-invoicing`; **Analyse** button parses the workbook and sums all fields (freight vs fulfilment, per-sheet totals, grand total by currency, PDF reconcile); drag-&-drop upload; totals-row exclusion; left-aligned.
**REPORTS ▸ Markdown & EOS** (509): pill-style filters + default UK / Markdown / Seasonal.
**FBA grid** (500/502/504): Carton qty after Disc; "3PL Inbound" last column with per-PO hover; select-all alignment/toggle + group borders; medium-blue headers.
**SUPPLY** (488–499): **SHIPPED TO MASTER** status + action (moved into DATES sub-tab, PO status dropdown replicated there); Payments Due "Likely pay date" (editable for overdue); Deposits silent save; Payments Report non-USD highlight + mobile scroll; PO drawer no longer hijacks the route; awaiting-confirmation "Send timeline message to supplier"; proposed-completion delta.

## Testing notes (verified in sandbox)
- 3PL parse of `DOC - May 26.xlsx`: Shipping Fee **20,561.57**, Storage **5,379.27**, Total Cost 31,304.04 (totals rows excluded).
- Revenue quarterly + Summary per-subcategory target saves return `{ok:true}` after migs 174/175.
- Buy-plan output unaffected (no engine changes in this bundle).

## Notes for Diviyaj
- Route order: `/api/demand-revenue-targets/periods` sits under the existing `/api/demand-revenue-targets` allowlist prefix — no auth-gate change needed.
- The 3PL parser is read-only (no writes); Cin7 order→CostCenter mapping (Phase 2) and Xero bill (Phase 3) are **not** in this bundle.
