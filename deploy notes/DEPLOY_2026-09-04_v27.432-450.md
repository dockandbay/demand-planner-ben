# DEPLOY 2026-09-04 — v27.432 → v27.450 (for Diviyaj)

**From Ben.** Stacks on **DEPLOY_2026-09-03_v27.389-431.md** (which ended at v27.431, migs 254–258).
This note takes live **v27.431 → v27.450**.

- **Schema migrations to apply: `259`, `260`, `261`, `262`** — all additive (`CREATE TABLE / ADD COLUMN IF NOT EXISTS`). Details below.
- **Data fixes to run: `fix_planner_created_by`, `fix_phantom_zero_forecasts`** (the phantom-zero one is **⚠ buy-affecting**).
- **Optional seeds (Ben's call): `seed_pantone_tcx_bootstrap`, `seed_pantone_coated_bootstrap`, `seed_timeline_phrases`.**
- **No new env vars. No new npm deps** (`pdf-lib` already present).
- **Files:** `server.mjs`, `supply/inject.html`, `artifact_v16.7.html`.
- **Buy-plan sensitivity:** the only buy-affecting item is the **phantom-zero fix (v27.437)** — see Post-deploy watch. Everything else is PRODUCT / samples / timeline / barcodes / config / display.

---

## Migrations (apply in order)

**259_pantone_colors.sql — APPLY.** New `planner.pantone_colors (code,name,hex,book,sort,active, UNIQUE(code,book))` + `planner.supplier_notes.pantone jsonb`. Backs the PRODUCT-timeline Pantone reference cards (`/p` search → inline swatch chips).

**260_barcode_projects.sql — APPLY.** New `planner.barcode_projects (name, target, batch, overrides jsonb, …)`. Backs SUPPLY ▸ Barcodes ▸ Customise (per-SKU barcode-number overrides, saved as projects, per-project batch). Includes an `ADD COLUMN IF NOT EXISTS batch` for safety.

**261_component_types.sql — APPLY.** New `planner.component_types (name, default_supplier, sampling_mode, sort, active, UNIQUE(name))`. The PRODUCT component catalogue (CONFIG ▸ Components). Empty on live — Ben populates via the UI (sandbox has a starter set: Product body / Pouch / Box→MQ Print / Hang tag→MQ Print / Care label→MQ Print / Polybag→spec-linked).

**262_product_dev_components.sql — APPLY.** New `planner.product_dev_components (item_ref, component_type_id, name, supplier, sampling_mode, spec_id, sort, UNIQUE(item_ref,name))`. Per-product component set (PRODUCT ▸ [product] ▸ Components). Additive; nothing existing reads it.

## Data fixes

**fix_planner_created_by_2026-09-04.sql — RUN (data cleanup).** Relabels sample_requests / notes wrongly attributed to the literal `'planner'` → **'Dock & Bay'** (root cause fixed in code v27.433). ~9 requests + 8 notes. Safe, idempotent.

**fix_phantom_zero_forecasts_2026-09-04.sql — RUN (⚠ BUY-AFFECTING).** Clears the all-zero `review_ui` `forecast_outputs` for in-scope SET SKUs with no sales history whose rows are entirely zero. Those phantom zeros were suppressing the auto-forecast (root cause fixed in code v27.437 — `buildSkuChanges` no longer writes null→0). **Live scope ≈ 25 new sets / 1,966 rows.** After this, those sets auto-forecast → their components enter the buy. Only touches sets that have NO real saved forecast.

## Optional seeds (Ben decides)

- **seed_pantone_tcx_bootstrap** (2,310 FHI TCX) and **seed_pantone_coated_bootstrap** (1,341 Coated) — community-sourced, no open licence, approximate HEX, internal reference only. Give the `/p` search its library. Skip if Ben would rather seed only from the Pantone Connect export.
- **seed_timeline_phrases** — 5 pre-written PRODUCT-timeline quick phrases (Ben). Idempotent.

---

## What changed (by area)

### PRODUCT timeline (v27.430–435, 444, 448) · migs 258 (prior note) + 259
Tags & badges + quick phrases (CONFIG-managed) with a **/** phrase picker and a **/p** Pantone picker; Pantone renders as an **inline swatch chip** (hover shows the large card); coloured tag badges + used-tag filter; **retag an already-sent message**; **native spellcheck** on the compose box; **newest-first** sort fixed; inline **retag**.

### PRODUCT ▸ Config (v27.434, 441, 443, 449) · mig 261
Moved to a **PRODUCT ▸ Config** tab (out of ⚙ CONFIG) with the app-standard **L3 pill menu**: Season/release · Categories · **Components** · Timeline tags · Timeline phrases. Categories gained **add/delete** + a real auto-derived code placeholder. **Components** = the new catalogue (mig 261).

### PRODUCT ▸ Components tab (v27.450) · mig 262
Per-product component set (Component · Supplier · Sampling · Status · Files); supplier per component (= product supplier / override); Spec-linked → "no sampling" + spec link in Files. First slice of the components-per-supplier redesign.

### PRODUCT ▸ Samples (v27.445–446, 448) · no migration
Printable **sample card** per version — downloads as a **PDF** (`GET /api/product/sample/:id/card.pdf`, pdf-lib): Supplier · Season · Product type · Colour · Reference (title) · Sample version · Date; STATUS tick-boxes from the per-aspect sign-off; blank comment/approval lines if printed pre-review. Sample-requests table shows **carrier + a dynamic tracking link**.

### SUPPLY ▸ Barcodes ▸ Customise (v27.436, 438–440) · mig 260
🎨 Customise drawer: import **SKU,barcode** overrides, **per-SKU P/C/I** ticks, **download all / per-row in the drawer**, save/load as **projects** with a **batch**; any number renders EAN-13 or **Code128** (new encoder). Distinct from EDI/retailer labels.

### FIX — new-set forecast (v27.433, 437)
sample-create no longer stamps `'planner'` (captures the real user). `buildSkuChanges` no longer writes a phantom **0** for a dirty-but-uncomputed SKU×month (that saved 0 was beating the auto-forecast). Cleanup SQL clears the existing phantom zeros — see Data fixes.

### REPORTS ▸ 3PL Forecast (v27.447) · no migration
Sub-pallet consignments (0<pallets<1) now labelled **LCL** (blue), not "air" — real air only when the ship id / PO ref says air. Server returns a `fill` field.

### Smaller
- v27.442: removed the per-file version input on component files.

---

## Post-deploy watch
- **After `fix_phantom_zero_forecasts` (buy-affecting):** ~25 new SET SKUs begin auto-forecasting → their components appear in the buy. Verify the buy-plan total moves only on those sets (and is sane).
- After migs 259–262: `/p` Pantone works in the product timeline; Barcodes ▸ Customise saves/loads projects; CONFIG ▸ Components + the product Components tab render.
- If the Pantone seeds are applied, `/p` returns the TCX + Coated libraries; if not, only whatever's seeded from Connect.
