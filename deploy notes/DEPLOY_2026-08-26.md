# DEPLOY 2026-08-26 — HORIZON (for Diviyaj)

**Package:** LIVE is **v27.275** → deploy to **v27.297** · Branch: `phase-2.1-suppliers` · From: Ben

**Migration: `244_edi_projects.sql` only.** (243 already run on live 26-Aug.)
**New dependencies — run `npm install` on pull:** `pdf-lib`, `jszip`, `pdfjs-dist` (the last two were transitive before; now declared explicitly). `exceljs` / `pdf-parse` already present.
**No new env vars.** Files touched: `server.mjs`, `artifact_v16.7.html`, `supply/inject.html`, `package.json`, `package-lock.json`, `migrations/244_edi_projects.sql`, `CHANGES.md`, docs.

---

## ⭐ Action needed / deploy order
0. **PRODUCTION FIX (v27.276) — deploy this.** `queryCapped` now sets `idle_in_transaction_session_timeout='15s'` (the two bulk-write txns too). This is the root cause of the 22–24 Aug pool-timeouts you bandaged with the 60s reaper — deploying makes it self-heal (keep the reaper as backstop). **Not yet on live.**
1. `npm install` (new deps above).
2. Run **migration `244_edi_projects.sql`** (two tables for saved EDI uploads; harmless if the EDI feature is unused).
3. Deploy on Vercel.

## ⚠ Behaviour changes to be aware of (default-on)
- **v27.280 (buy-plan) — bad-PO exclude:** `PO-56UKXR2` / `PO-56UKXR2A` hard-coded out of every inbound/on-order query (`EXCLUDED_INBOUND_REFS`). `PO-56UKXR2A` = ~10,710 phantom unreceived units into `uk_3pl`; removing it **lowers on-order for ~29 SKUs** (they may buy more — intended). Better long-term fix is deleting/receiving the bad `inbound_shipments` rows at source, then the hard-code can go.
- **v27.293/294 (buy-plan) — seasonal block buy now covers FBA:** the windowed-range Complex Rule up-front block summed DTC+B2B only; now **DTC+B2B+FBA** (3PL feeds FBA via transfer). FBA-heavy SS27 SKUs now place their full season up-front in the window month instead of dribbling into later buys. Only SS27 range-rule SKUs with FBA demand change. (TikTok is already folded into DTC and Zalando has its own mechanism — deliberately not re-summed, to avoid double-count.)
- **v27.282 — FBA in-flight ETA:** a past/missing FBA ETA is now assumed **today+4 days** (display + ≤14-day bucketing).

Everything else is either UI-only or off-by-default (SSM still opt-in per market×pool).

---

## What's in this package (v27.276 → v27.297)

### Production stability
- **v27.276** — `queryCapped` idle-in-transaction guard (see Action 0). Also: `FAIRE-COMMISSION` added to `NON_SKUS` and the Forecast-trend-anomalies report skips £0-impact rows.

### Payments (`#/supply/payments`)
- **v27.277 / v27.278** — new default pill **"Outstanding +4 weeks"** = unpaid AND (due ≤ today+28d, incl. overdue) OR no due date. Payments Report ▸ Copy email first line → *"Payment detail - X of Y"*.

### Inventory Status Report (`#/buy-move/inventory/status`)
- **v27.279** — FBA/3PL rows **availability-scoped** to `v_product_availability` (not stale baked channels); new **Core/Seasonal** column; FBA oos/low get a **3PL SOH** column + all tables a **4wk fcst** column; **collapsible tiers** + first-5/**Load more** (no inner scroll); per-sub-header **⧉ Copy / ⤓ CSV**; top **⤓ Full XLSX report** (new `POST /api/supply/inventory-status/export.xlsx`, uses `exceljs`, read-only). §5 delays table left-aligned.

### Buy plan
- **v27.280** — bad-PO exclude (see ⚠) + exposed `BP.fbaTransferRec/…` diagnostics.
- **v27.282 / v27.283 / v27.284** — FBA in-flight branch transfers: shown in the "FBA inbound ≤14d" column; past ETA → today+4; in-flight popup gets click-away-close + sticky header + market filter; the Cin7 refresh widened from a 48h to a **30-day** `CreatedDate` window and now stores the real **`estimatedDeliveryDate`** (was `approvalDate`).
- **v27.285 / v27.286** — SKU-popup demand rows recoloured (DTC/B2B/FBA `#FDFBD4`, Total Demand `#FFFFC5`).
- **v27.288** — Create-PO price-break hint gets a per-SKU **"order extra N as China stock"** checkbox (opt-in; merges into the China-stock holding PO).
- **v27.290** — Create-PO XLSX download gains a **"Summary with Inbound"** tab (On hand 3PL+FBA / Inbound / Buy per country).
- **v27.293 / v27.294** — block buy covers FBA (see ⚠).
- **v27.295** — Buy 3PL cell tooltip shows the **block-buy calculation** (rule, cover-to-season-end, safety, − on-hand/inbound, carton rounding); fixed a cut-off inbound tooltip (`.tip-box` now wraps).
- **v27.296** — **Forecast model** surfaced: a popup card (Cover weeks vs SSM, "Mixed" if pools differ) + a Settings row summarising SSM markets with a link to `#/demand/config/buyplan`.

### EDI Labels — NEW feature (Dillards via EZY COM)
Hidden page `#/supply/purchase-orders/edi-labels`, reached from a **🏷 EDI LABELS** button on the Direct-to-Client report.
- **v27.287** — upload `wc-asn.csv` + SSCC label PDFs → server matches each label's SSCC (last-18) to the CSV → PO + our SKU (via EAN `products.product_ean`) + supplier, splits each page (`pdf-lib`) into **Supplier / PO / SKU / <sscc>.pdf**, returns a ZIP (`jszip`) + `manifest.csv`. Endpoints `POST /api/supply/edi-labels/{parse,generate}` (read-only, write-gate-exempt). **⚠ serverless:** parse→generate uses an in-memory job cache — fine on a persistent server, but on Vercel it won't survive between the two requests; needs a shared store or a single-request fold when wired to prod.
- **v27.288 / v27.289 / v27.291** — 7 numbered step boxes; per-PO **Preorder ref** as a searchable crossdock/preorder picker; multi-supplier **yellow dropdowns** (default main); unmatched → `_UNMATCHED/{_SSCC_NOT_IN_CSV,_EAN_NOT_MATCHED,_NO_SSCC_FOUND}`; per-step drop zones; **per-supplier packing-list CSV** (Supplier · PO · Preorder Ref · SKU · Qty · SSCC).
- **v27.292** — **Save as project** (migration 244): persists the uploaded CSV+PDFs (bytea) so users can return to them; file list shows upload time + uploader + ✕ remove + Clear all; endpoints under `/api/supply/edi-projects`.

---

## FYI — live data ops done this session (with Ben's approval, not code)
- **UK ILG July** Cin7 orders (20,100) already loaded to live earlier; sandbox re-seeded this session for testing.
- **Inventory snapshots:** 26-month history live (sharpens the SSM backtest).
- Ben's live DB password was shared in an earlier chat — **rotate it** if not already done.

## Deploy summary
Pull `phase-2.1-suppliers` @ **v27.297** → `npm install` → run **migration 244** → deploy on Vercel. Only new env/dep footprint is the three npm packages above.
