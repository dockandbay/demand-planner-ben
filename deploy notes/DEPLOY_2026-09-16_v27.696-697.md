# Deploy note — v27.696 → v27.736 (16-Sep-2026)

## AT A GLANCE (for Diviyaj)

**Repo/branch:** `dockandbay/demand-planner-ben` · `phase-2.1-suppliers` · HEAD **v27.736** (pushed). Live = v27.694.

**Migrations to apply (all additive; live PRODUCT/tracking tables are empty, so NO backfill):**
- **277** `planner.categories.colour_hex` (server auto-fills on first read)
- **278** `planner.product_dev_requests` + `product_dev_request_components`; `product_dev_samples.request_id/received_at`; `product_dev_items.stage` nullable
- **279** `planner.portal_attachments.is_latest/thumb/thumb_mime`
- **280** `planner.product_dev_size_dimensions.sampling_mode/spec_id`
- **281** `planner.carrier_tracking` (DHL tracking cache) — NEW this batch

**New env var:** `DHL_API_KEY` (optional). The DHL tracking feature is INERT until it is set: no external calls, poller returns "DHL_API_KEY not set". Ben supplies the key (DHL developer portal, "Shipment Tracking - Unified", basic plan). Put it in prod env only; never in DB/git. The consumer *secret* is not used (no OAuth). Also confirm the existing `N8N_WEBHOOK_SECRET` is set (it gates `POST /api/tracking/poll`, same pattern as received-pos).

**n8n:** add a schedule that calls `POST /api/tracking/poll` with header `x-webhook-secret: <N8N_WEBHOOK_SECRET>` every 1-2h. On Vercel the in-app timer does NOT run (guarded `!process.env.VERCEL`), so the schedule is what drives refreshes in prod. The poller self-throttles (<=1 req/sec, 200/run) and honours the config interval, so over-calling is harmless.

**New routes:** `/api/tracking/poll|test|status|config` (281); portal `POST /api/portal/parse-invoice` + `/invoice-apply` and `GET/POST /api/supply/po/:po/invoice-duplicates[/dismiss]` (707); `/vendor/pdfjs` static from `node_modules/pdfjs-dist` (708 — verify it resolves on Vercel).

**Deps:** none new (`pdfjs-dist` already in package.json; DHL uses Node's global fetch).

**Files touched:** `server.mjs`, `supply/inject.html`, `supply/portal-view.js`, `artifact_v16.7.html` (v27.696 only), `migrations/277..281`, `package.json`, `CHANGES.md`.

**Independent of** the still-pending `DEPLOY_2026-09-16_v27.695_storage-uploads.md` package (mig 276 + `horizon-uploads` bucket + `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`) — ship either order.

**Post-deploy smoke:** (1) `/api/version` = v27.736; (2) PRODUCT ▸ SAMPLING and a product detail render; (3) CONFIG ▸ Admin ▸ DHL tracking shows "API key present" once `DHL_API_KEY` is set, and "Run poll now" populates pills on SAMPLING + Shipments; (4) the storage/pdf.js checks from the sections below.

---


**v27.708-714 summary (see CHANGES.md):** **708 product Documents versions/thumbnails — MIGRATION 279** (`portal_attachments.is_latest/thumb/thumb_mime`; serves pdf.js from `/vendor/pdfjs` out of `node_modules/pdfjs-dist` — check the static route resolves on Vercel) · 709-711 PLAN→PRODUCTS rename (`#/product/products`), swatch cache-bust · 712 product detail polish + component actions only while a request is in review · **713 sampled vs spec-linked per size × component — MIGRATION 280** (`product_dev_size_dimensions.sampling_mode/spec_id`, additive, no backfill), Sample requests column, spec link/upload from the grid, `comp:<id>` dimensions accepted by `POST /api/product/size/:id/dimension` + `/api/product/component-file` · 714 SAMPLING grid layout; Sample batch review is now a view inside SAMPLING (L2 tab removed; `#/product/batch/<sr>` still resolves) · 715-717 SAMPLING column/tab layout + `#/product/sampling/<request>/S<n>` deep links (client only). **Migrations for live: 277, 278, 279, 280** (all additive). No new env vars; `pdfjs-dist` is already in package.json.

**v27.701-707 summary (see CHANGES.md):** 701 rich-compose first-pick fix · **702 PRODUCT split P1 server — MIGRATION 278** (`product_dev_requests`, `product_dev_request_components`, `product_dev_samples.request_id/received_at`, `product_dev_items.stage` nullable; live PRODUCT tables are empty → no backfill) · 703 PRODUCT grid on requests · 704 SAMPLING page (`GET /api/product/sampling`, `POST /api/product/sample/:id/received`) · 705 portal Productions Total column · 706 timeline change-grouping · **707 invoice duplicate-SKU summing + warnings; NEW portal routes `POST /api/portal/parse-invoice` + `/api/portal/invoice-apply`** (the live portal previously had no endpoint behind its Parse-invoice button) + `GET/POST /api/supply/po/:po/invoice-duplicates[/dismiss]`. Migrations for live: **277, 278**. No new env/deps.

Also in this range (client-only, no migration): **v27.699** PRODUCT plan group headers flush left; **v27.700** PRODUCT split P0 — rich compose box (contenteditable over the hidden textarea) rendering Pantone tokens as inline swatch + code + name chips on the product Timeline, Samples-tab and Batch Review feedback boxes; server untouched, note bodies keep the same "🎨 <code>" text.

Stacks on `DEPLOY_2026-09-16_v27.695_storage-uploads.md` (v27.695, still pending its bucket + env). These three can ship independently of v27.695. **ONE migration: `277_category_colour.sql`** (additive, `planner.categories.colour_hex`; the server fills colours on first read, nothing to seed). No new env vars, no new npm deps, no buy-plan impact.

Files: `server.mjs`, `supply/inject.html`, `supply/portal-view.js`, `artifact_v16.7.html` (v27.696 only), `migrations/277_category_colour.sql`, `package.json`, `CHANGES.md`.

## v27.736 Fulfil ERP: PO create into the SANDBOX enabled + verified (SERVER only, no migration)
- Fulfil auth = `X-API-KEY` header (verified with a fresh sandbox key). `purchase.line` now sends the required `unit` (product purchase UOM) + numeric `unit_price` (null->0). `FULFIL_LINES_SEND=true` (real create/update). Created PO-78AUWK1 in the sandbox (id 242, USD). SANDBOX-only: inert on prod unless Active ERP=Fulfil AND `FULFIL_LIVE_*` keys set. New sandbox `FULFIL_SANDBOX_API_KEY` is in Ben's .env; Diviyaj needs a `FULFIL_<ENV>_API_KEY` for prod when going live. File: `server.mjs`, `package.json`, `CHANGES.md`.

## v27.735 SAMPLING: deep links to the MANAGE panel + tab (CLIENT only, no migration)
- `#/product/sampling/<ref>/<tab>` (master-data|timeline|S1..) opens MANAGE on that tab; URL follows the open tab. File: `supply/inject.html`, `package.json`, `CHANGES.md`.

## v27.734 SAMPLING: MANAGE Master tab read-only PRODUCT master + files + product link (CLIENT only, no migration)
- MANAGE Master tab now read-only (product master + files + 'Open product ↗' link) instead of the editable form. File: `supply/inject.html`, `package.json`, `CHANGES.md`.

## v27.733 SAMPLING: MANAGE tabs use the shared .po-subnav/.rtab tab UI (CLIENT only, no migration)
- MANAGE tabs now match the PO drawer / samples grid underline-tab style. File: `supply/inject.html`, `package.json`, `CHANGES.md`.

## v27.732 SAMPLING grid: MANAGE button black/white + tighter swatch gap (CLIENT only, no migration)
- Manage button black bg/white text; removed the white space between the swatch and the PRODUCT column. File: `supply/inject.html`, `package.json`, `CHANGES.md`. No env/deps/migration.

## v27.731 P2b DHL tracking: "mark received" from the delivered tracking log (SERVER + CLIENT, no migration)
- `GET /api/tracking/detail` also returns the linked development sample + received state; the tracking log shows a one-click "✓ Mark sample received" button when Delivered + not yet received (admin only, never automatic). File: `server.mjs`, `supply/inject.html`, `package.json`, `CHANGES.md`. No env/deps/migration.

## v27.730 SAMPLING: MANAGE button — inline tabbed workspace (CLIENT only, no migration)
- Each SAMPLING row gets a ⚙ Manage button → inline tabbed panel (Master data · Timeline · one tab per sample). Reuses `renderProdMaster` / `renderProdTimeline`; per-sample tabs mirror the inline sample review. Also repairs an empty `package.json` (a v27.729 in-place edit truncated it). File: `supply/inject.html`, `package.json`, `CHANGES.md`. No env/deps/migration.

## v27.729 FIX: sample received record-of-change date -> dd-mmm-yy (SERVER only, no migration)
- 'Sample vN received' change-log line now formats the date dd-mmm-yy (was ISO). File: `server.mjs`, `package.json`, `CHANGES.md`.

## v27.728 SAMPLING grid: remove Age; widen Stage (CLIENT only, no migration)
- Age removed from the SAMPLING grid; Stage column/select widened so the dropdown never clips. File: `supply/inject.html`, `package.json`, `CHANGES.md`. No env/deps/migration.

## v27.727 P2b DHL tracking: clickable tracking LOG (SERVER + CLIENT, no migration)
- Every DHL pill (SAMPLING, Shipments grid, PO drawer Shipments tab, Samples card, portal) opens a tracking-log overlay: status, key delivery date (delivered or ETA), and the full cached event history. New on-demand endpoints `GET /api/tracking/detail?number=` (admin) and `GET /api/portal/tracking-detail?number=` (supplier-scoped). File: `server.mjs`, `supply/inject.html`, `supply/portal-view.js`, `package.json`, `CHANGES.md`. No env/deps/migration.

## v27.726 P2b DHL tracking: supplier portal Samples pills (SERVER + CLIENT, no migration)
- New supplier-scoped route `GET /api/portal/tracking-status` (portalAuth) returns cache rows only for the signed-in supplier's own sample/shipment numbers. `portal-view.js` shows a DHL pill next to the sample tracking code (status word localised via PP_ZH). File: `server.mjs`, `supply/portal-view.js`, `package.json`, `CHANGES.md`. No env/deps/migration.

## v27.725 P2b DHL tracking: poller hardening (SERVER only, no migration)
- Validated live against the real DHL API (Ben's key): delivered status + shipment `tracked_delivery_date`/`tracked_source='dhl'` write-back confirmed. Poller now sanitizes/splits `carrier_ref` numbers (skips junk like "AIR"), **skips completed/arrived shipments and received samples**, and trims stored `events` (was 55KB for a multi-piece delivery, now ~1.3KB). File: `server.mjs`, `package.json`, `CHANGES.md`. No env/deps/migration (the `DHL_API_KEY` from v27.718 still applies).

## v27.724 FIX: sample escalate found no portal users (supplier_id null) (SERVER only, no migration)
- **User-facing bug on live:** escalating a sample from the timeline emailed nobody ("No active portal users for this supplier") for every supplier, because all `sample_requests` rows have `supplier_id` NULL and the escalate resolved recipients by id only. `escalateCore` now falls back to resolving by `supplier_name` -> `suppliers.id`; admin sample-create now sets `supplier_id` from the name. No migration, no backfill needed (the code fix handles existing null-id rows). Optional cleanup for Diviyaj: `UPDATE planner.sample_requests sr SET supplier_id=s.id FROM planner.suppliers s WHERE sr.supplier_id IS NULL AND lower(s.name)=lower(sr.supplier_name);` File: `server.mjs`, `package.json`, `CHANGES.md`. **Worth deploying promptly** — sample escalate is currently non-functional on live.

## v27.723 P2b DHL tracking: pills on PO-drawer shipments + Samples card + auto-fill observer (CLIENT only, no migration)
- A debounced MutationObserver auto-fills any `.hz-trkpill` placeholder from the cache; pills added to the PO drawer Shipments tab and the SUPPLY Samples card. DHL carriers only. Client-only. File: `supply/inject.html`, `package.json`, `CHANGES.md`. No env/deps/migration.

## v27.722 PRODUCT: Timeline moved onto SAMPLING, removed from product detail (CLIENT only, no migration)
- Timeline now expands inline on the SAMPLING row (reuses `renderProdTimeline`); Timeline tab removed from the product detail drawer. `#/product/<ref>/timeline` deep link still resolves. Client-only. File: `supply/inject.html`, `package.json`, `CHANGES.md`. No env/deps/migration.

## v27.721 P2b DHL tracking: pills on the SUPPLY Shipments grid (CLIENT only, no migration)
- Shipments grid Carrier-ref cell shows a DHL status pill from the cache (fills after full paint and after the in-place row patch). DHL carriers only. Shares the poller's `tracked_delivery_date` write-back. Client-only. File: `supply/inject.html`, `package.json`, `CHANGES.md`. No env/deps/migration.

## v27.720 P2b DHL tracking: pills on the SAMPLING Shipment cell (CLIENT only, no migration)
- Reusable pill helper (`hzFillTrackPills`) reads `GET /api/tracking/status` and fills `.hz-trkpill` placeholders; the SAMPLING grid Shipment cell shows a DHL status pill (in transit + ETA / delivered / exception). DHL carriers only; empty until the cache has rows (once `DHL_API_KEY` is live). Client-only. File: `supply/inject.html`, `package.json`, `CHANGES.md`. No env/deps/migration.

## v27.719 P2b DHL tracking: CONFIG ▸ Admin ▸ DHL tracking panel (CLIENT only, no migration)
- New admin sub-tab under CONFIG ▸ Admin: enabled toggle, refresh interval / in-transit interval / stop-days, Save, "Run poll now", and a test-a-tracking-number box. Reads `key_present` from env to show a present / not-set indicator. Client-only; wires to the v27.718 `/api/tracking/*` routes. File: `supply/inject.html`, `package.json`, `CHANGES.md`. No new env/deps/migration beyond v27.718.

## v27.718 P2b DHL tracking: carrier_tracking cache + poller core (SERVER + MIGRATION 281)
- **NEW migration `281_carrier_tracking.sql`** (additive): `planner.carrier_tracking`, one row per tracking number, shared by sample shipments (`sample_requests.tracking_code[_2]`) and bulk shipments (`shipments.carrier_ref`). No backfill, no seed.
- **NEW env var `DHL_API_KEY`** (optional). The whole feature is INERT until it is set: no external calls, and the poller returns "DHL_API_KEY not set". Put the key in prod env only (Ben supplies it from the DHL developer portal); never in the DB or git. Poller auth reuses the existing `N8N_WEBHOOK_SECRET`.
- **NEW routes:** `POST /api/tracking/poll` (n8n schedule, `x-webhook-secret` gated exactly like `received-pos/process`), `POST /api/tracking/test` (admin, live single lookup, no cache write), `GET /api/tracking/status?numbers=` (read-only cache for the UI pills), `GET`/`POST /api/tracking/config` (admin; stored in `app_settings.dhl_tracking`).
- **n8n schedule (Diviyaj):** call `POST /api/tracking/poll` on a schedule (every 1-2h is plenty) with the webhook secret header. On Vercel the in-app timer does not run (guarded `!process.env.VERCEL`), so in prod the schedule is what drives refreshes. The poller self-throttles (<=1 req/sec, 200 calls/run) and honours the config interval, so over-calling the schedule is harmless.
- **Bulk shipments:** when a shipment's DHL number resolves, the poller sets `shipments.tracked_delivery_date` + `tracked_source='dhl'`, which the existing arrival logic already consumes. No other shipment behaviour changes.
- No new npm deps (Node global fetch). Files: `server.mjs`, `migrations/281_carrier_tracking.sql`, `package.json`, `CHANGES.md`.
- Sandbox already holds 6 DHL sample numbers + 134 DHL shipment numbers, so the first live poll has real data. UI tracking pills + the CONFIG panel follow in v27.719-721 (same package).

## v27.698 — PRODUCT category colours (SERVER + CLIENT + MIGRATION 277)
- Each `planner.categories` row gets a `colour_hex`, auto-assigned server-side from a fixed 19-colour palette the first time it is read without one (`ensureCategoryColours`, at most one scan a minute; n8n-added categories get coloured automatically). Editable in PRODUCT ▸ Config ▸ Categories (`POST /api/product/category-colour`).
- Coloured category badges on the PRODUCT grid (rows + group headers), phone cards, Range plan and Dashboard. The product grid also loses its Age and Sample-shipment columns (Ben).
- Post-deploy: open PRODUCT ▸ Plan — every category header and row shows a coloured badge; PRODUCT ▸ Config ▸ Categories shows the colour pickers.

## v27.696 — 3PL invoice (EU iFulfilment): stop double-counting Consumables (SERVER + CLIENT)
- **Bug:** Aug-26 EU reconcile grand total read EUR 47,531.37 vs invoice INV-192928 net 46,190.59 — over by exactly the Consumables line (1,340.78).
- **Cause:** from the Aug-26 export iFulfilment's "Total Excl Shipping" column already INCLUDES "Consumables Cost" (Jul-26 and earlier it did not). The v27.258 fold that added consumables on top now double-counted.
- **Fix:** `_tplConsumablesInside(headers, sumOf)` decides per order sheet from the column sums and skips the fold when consumables are already inside. Applied to both the Analyse grand total (`_tplGridSummary`) and the per-order fulfilment used by map / journal / Xero bill (`_tplOrderRows`). Parsed summary shows a per-sheet "Consumables: N (inside / outside)" note.
- **Verified** on both live workbooks: Jul-26 unchanged (48,259.51), Aug-26 = 46,190.59 = invoice net. Other 3PLs have no such column → unchanged.
- **Interim:** Ben has the corrected Aug-26 Xero bill + journal CSVs already (generated from the fixed sandbox against mirrored live orders), so nothing to re-run after deploy unless the file is re-analysed.

## v27.697 — Barcodes: order-plan "Download barcodes ▾" + portal production download fixes (SERVER + CLIENT)
- **Admin:** PO drawer ▸ Order plan gains "⤓ Download barcodes ▾" (All / Product / Carton + Inner) next to "⬆ Push to ERP (full)". Uses `GET /api/supply/label-data?po=` (existing; stamps the PO's batch + production date) and the existing `bcDownloadAll` A4 pipeline. No new endpoint.
- **Portal bug (PO-57AUBL1 "Download barcodes for 57"):** BATCH / DATE OF PRODUCTION were blank because `?prod=` never stamped a batch. The button now sends `batch_po=<PO>`; `GET /api/portal/label-data` stamps that PO's batch (ownership-checked via `portalOwnsPO`), with a fallback to the single batch shared by the supplier's POs in the production. New optional query param only; existing calls unchanged.
- **Label builders unified:** portal `buildCartonSVG`/`buildLabelSVG` replaced with the admin versions (logo gap that fixed the squashed header, part-number/description lines, per-market RRP); admin `lblCircle` takes the portal's PIM-typo guard. Both generators are now identical.

## Post-deploy checks
1. REPORTS ▸ 3PL & Invoicing ▸ EU iFulfilment ▸ Aug-26 ▸ Analyse: grand total EUR 46,190.59; Orders sheet shows "Consumables: 1,340.78 (inside Excl Shipping — not added again)". Jul-26 still 48,259.51 with "(outside — added to total)".
2. SUPPLY ▸ Purchase Orders ▸ open PO-57AUBL1 ▸ Order plan: "⤓ Download barcodes ▾" → Carton + Inner → ZIP of A4 PDFs, labels carry BATCH 57.0626.
3. Supplier portal (ACME / PO-57AUBL1) ▸ Barcodes & Labels ▸ "⤓ Download barcodes for 57": carton labels show BATCH 57.0626 + date, header spacing matches admin output.
