# DEPLOY 2026-08-25 — HORIZON (for Diviyaj)

**Package:** LIVE is **v27.275** → deploy to **v27.277** · Branch: `phase-2.1-suppliers` · From: Ben

**Migration: `243_launch_ramp.sql` only** (adds `buy_complex_rules.ramp_months`, `ramp_sl`). **No new env vars. No new dependencies.** Standard pull + Vercel deploy + run migration 243. Files touched: `server.mjs`, `artifact_v16.7.html`, `package.json`, `CHANGES.md`, docs.

> v27.262–v27.275 are already on live (Diviyaj deployed v27.275 + ran mig 243, 26-Aug). This package = **v27.276 + v27.277** (both migration-free, UI/DB-safe).

**v27.277 (payments UI, no migration):** `#/supply/payments` gains a new default pill **"Outstanding +4 weeks"** (unpaid AND due ≤ today+28d, overdue included; no-due-date rows excluded); old pills unchanged. Payments Report ▸ Copy email first line reworded to *"Payment detail - X of Y"*.

---

## ⭐ Action needed
0. **PRODUCTION FIX (v27.276):** `queryCapped` now sets `idle_in_transaction_session_timeout=15s` — this is the source of the 22–24 Aug pool-timeout that Diviyaj bandaged with the 60s reaper. Deploying this makes it self-heal (the reaper can stay as backstop).
1. ~~Run migration `243_launch_ramp.sql`** on live before/with the deploy (the Complex Rules launch-ramp type reads these columns; harmless if unused).
2. **(If not already done under v27.258)** re-run **REPORTS ▸ 3PL Invoice ▸ EU iFulfilment** for July (and worth checking prior months) so past bills pick up the **Consumables** line now that it's booked.

Nothing else is behaviour-changing by default: SSM is still opt-in per market×pool (off), and the Complex-Rules changes only affect SKUs a rule matches (SS27).

---

## What's in this package (v27.262 – v27.275)

**3PL Invoicing**
- **v27.262 / v27.263 — unmapped freight auto-books to "Other"** for every 3PL (iFulfilment/Coghlans → Other Costs; Geneva → Geneva Other fees; ILG → Other Fees), so the bill always balances (no "COST CENTRE MISSING" gap). New amber **"could not reconcile"** warning box lists the amount + exact unreconciled references; the Clean-up sweep is still offered.

**Buy plan — Complex Rules + SSM**
- **v27.264 — windowed "range" rule = up-front block buy.** A range rule with a window now places one up-front order at the window start (e.g. Sep 1) for the whole range, instead of per-month cover raises that a long lead pushed into the past (which landed the buy late). Depends on `planner.buy_complex_rules` (already live).
- **v27.265 — the block is SSM-sized:** season-detected coverage (captures the full sell-out) + tier-aware SSM safety stock (`ssmSafetyUnits`). `ssmCoverWeeks` untouched.
- **v27.266 — Launch/NPD ramp Complex Rule (option B):** new `launch_ramp` coverage type — elevated SSM service level for N months post-launch, decaying to the tier SL. Rule editor + server + **migration 243**. Regression-safe (inert when no such rule). Known limit: SSM maths are "now"-anchored, so a launch >~6mo out is inert until closer (launch-anchored SSM is a planned follow-up).

**Inventory Status Report (SUG-0031 / SUG-0032)**
- **v27.267 —** inbound column shows the **next-shipment PO** (hyperlinked to the PO drawer); FBA rows show **FBA-specific inbound + on-order**; the Buy-Plan snapshot-upload form clears on success.

**SSM Backtest / Buy Plan config (`#/demand/config/buyplan`)**
- **v27.268 / v27.269 —** fixed the cut-off Recommendation column; widened it and tightened the metric columns (fixed layout + colgroup).
- **v27.270 — Cashflow Compare (Standard vs SSM)** popout: runs the buy plan under both models, values buys at unit cost (`/api/supply/products-all`), shows GBP committed per order month (Standard / SSM / Δ / cumulative + totals).
- **v27.271 —** Cashflow Compare: month labels `Mmm YYYY`, capped to the next **12 months**, applies the 1b after-the-20th roll (current month's orders roll to next; current month empty past the 20th).

**Forecast quality**
- **v27.272 — DEMAND ▸ Analysis ▸ Forecast Anomalies (new report):** SKU×country×channel with actuals diverging from forecast for 2+ complete months (hot/cold), forward-caught-up flag, £-ranked. New endpoint `/api/demand/forecast-anomalies`; no migration.
- **v27.273 —** Forecast anomalies renamed to **Forecast trend anomalies**; columns centred (SKU/Subcat left + widened), month cells show forecast-over-actual (green/red).

**3PL invoicing**
- **v27.274 —** 3PL invoice upload accepts **multiple files** (multi drag-and-drop / multi-select), uploaded sequentially. UK ILG already combines all uploaded sheets in the allocation.
- **v27.275 —** cross-month reclass **journal now works for UK ILG** (was empty — generic parser couldn't read ILG columns); all 4 3PLs now produce a journal.

---

## FYI — live data ops this session (with Ben's approval, not code)
- **Cin7 InvoiceDate backfill (EU iFulfilment)** where dispatched orders had null invoiceDate → set = dispatchedDate (Jun/Jul/Aug). Worth fixing at source or via a daily n8n backfill.
- **UK ILG July** loaded into live `planner.tpl_cin7_orders` (20,100 orders) + the shipping-detail file + a cross-month clean-up sweep (via SQL Ben ran).
- **Inventory snapshots:** live now holds a full 26-month history (2024-07 → 2026-08), which sharpens the SSM backtest (e.g. US 3PL recommendation moved 99% → 95%).

## Deploy
Pull `phase-2.1-suppliers` through **v27.275**, run **migration 243**, deploy on Vercel. No env / dependency changes.
