# Deploy note — v27.742 → v27.751 (17-Sep-2026)

Covers the ERP two-button/tickbox finish, the PRODUCT split **P4 (QR + phone-scan)**, **P5 (drop moved item columns)** and **P6 (timeline /s sample chip)**. Continues from `DEPLOY_2026-09-16_v27.696-741.md` (migs 277-283).

---

## AT A GLANCE

**Repo/branch:** `dockandbay/demand-planner-ben` · `phase-2.1-suppliers` · HEAD **v27.751** (pushed). Live = v27.694.

**Migrations to apply — READ THE ORDER NOTE ON 285:**
- **284** `planner.product_dev_samples.short_code` (3-char code for the sample-card QR; the migration self-backfills existing rows). Additive.
- **285** **DROP** `planner.product_dev_items.{supplier, supplier_code, recipient_countries, approval_method, dev_start_override}`. ⚠ **Destructive. Deploy the v27.749 code FIRST, then run 285.** The code stops reading/writing those columns and derives them from `product_dev_requests`; running 285 before the code is live would 500 the old code. There is no backfill needed (the request model already carries the values, mig 278).
- **286** `planner.supplier_notes.sample_id` (+ partial index). Additive; links a timeline note to a dev sample.

**Env vars:**
- **`PORTAL_URL`** — please CONFIRM it is set to the real supplier-portal URL in prod. The sample-card QR encodes `PORTAL_URL/#/product/scan/<code>`, so a supplier scanning their card must land on the live portal. (Wrong/empty PORTAL_URL = the QR points nowhere.)
- No *new* env var is introduced by this batch. `DHL_API_KEY`, `N8N_WEBHOOK_SECRET`, `FULFIL_*` are unchanged from the prior notes (see Open Items §3-4).

**New file:** `lib/qrcode.mjs` — a small dependency-free QR encoder (Nayuki byte-mode port, no CDN, China-safe). Server-side only (imported by the sample-card PDF). **No npm dependency added.**

**New routes:**
- `GET /api/product/scan/:code` (admin) and `GET /api/portal/scan/:code` (portal, ownership-guarded) — resolve a scanned/typed sample short code → the phone sample-card payload.
- `GET /api/portal/product-swatch/:ref` (portal, ownership-guarded) — swatch image for the portal phone card.

**Deps:** none new.

**Files touched:** `server.mjs`, `supply/inject.html`, `supply/portal-view.js`, `lib/qrcode.mjs` (new), `migrations/284..286`, `package.json`, `CHANGES.md`.

**Post-deploy smoke:**
1. `/api/version` = v27.751.
2. PRODUCT ▸ SAMPLING renders; open a sample card PDF (`/api/product/sample/<id>/card.pdf`) — it has a QR + 3-char code top-right.
3. Scan/type a code in the barcode scanner → the phone sample card opens (not a SKU lookup).
4. PRODUCT ▸ a product detail + the items grid render (supplier now shows the request supplier(s), e.g. an aggregate like "ACME Ltd, XR Textile" on a shared product).
5. Portal (as a flagged supplier): their product opens; the master card no longer shows Supplier/Recipient/Approval rows; scanning their card opens the portal phone card with the swatch.
6. Product Timeline compose: `/s` lists sample versions; posting a note with a 🧪 token surfaces it on that sample's review thread.

---

## ⭐ OPEN ITEMS FOR DIVIYAJ (please assist)

Grouped by area, most-blocking first. Items marked **[needs Ben]** are waiting on an input from Ben before you can finish them — flagged so you know the dependency.

### 1. Deploy this batch + migrations
- Deploy through **v27.751**; run migs **284** and **286** (additive, any time) and **285** (**only after the v27.749 code is live** — see the order note above).
- Confirm **`PORTAL_URL`** in prod (QR target).

### 2. Large-file uploads (Supabase Storage) — the >60MB requirement
Canonical spec: `deploy notes/DEPLOY_2026-09-16_v27.695_storage-uploads.md`. Still **not live**. Two parts:
- **You:** create the **private bucket `horizon-uploads`** (100MB file-size limit) on prod ref `oolwklahstnvocaugryg`, and set prod env **`SUPABASE_URL`** + **`SUPABASE_SERVICE_ROLE_KEY`**. No RLS (the server mints signed URLs with the service key). Run mig **276** if not already applied.
- **[needs Ben]:** the same key pair for the **sandbox** (`mgqrcupazffvpzpeuxzt`) so the >3MB round-trip can be tested end-to-end before prod. Until then large uploads return a clean 503; small uploads are unaffected.
- Mechanism recap: files >3MB `PUT` **direct to Storage** via a signed URL (bytes bypass the Vercel ~4.5MB function cap); ≤3MB stay inline as before. Ceiling is 100MB today — raise `STORAGE_MAX` + the bucket limit if Ben wants >100MB.

### 3. Fulfil ERP go-live (Cin7 → Fulfil, running in parallel first)
The integration is built + sandbox-verified (PO create/update, mirror + drift, grid Cin7/Fulfil columns, two-button push, "not required in Cin7" tickbox). To take it live:
- **Set prod env:** `FULFIL_LIVE_SUBDOMAIN` and `FULFIL_LIVE_API_KEY` (env keys resolve as `FULFIL_<ENV>_SUBDOMAIN` / `FULFIL_<ENV>_API_KEY`; `<ENV>` = the Active-ERP env from CONFIG ▸ General settings). Sandbox pair is `FULFIL_SANDBOX_*`.
- **Seed the production supplier/branch Fulfil ids** — run **`POST /api/supply/fulfil/seed-ids?env=live&apply=true`** once in prod (with the live keys). It resolves every active supplier → `party.party` and every branch → `stock.location` by exact name and upserts `suppliers.fulfil_id` / `branches.fulfil_id`; dry-run first (no `apply`) to eyeball the match table + unmatched list. The party/warehouse ids in the sandbox DB are **sandbox** ids — do not carry them to prod. Currency/country still resolve by code at push time.
- **n8n cron:** add a schedule that calls **`POST /api/supply/fulfil/import-pos`** (gated by `N8N_WEBHOOK_SECRET`, header `x-webhook-secret`) to mirror all Fulfil POs into `planner.fulfil_purchase_orders` (mig 282) for drift. On Vercel the in-app 6h timer is guarded off (`!process.env.VERCEL`), so n8n is what drives it in prod.
- **[needs Ben]:** confirm the go-live date / the ~2-week parallel window before Cin7 is switched off.

### 4. DHL tracking go-live
- **`DHL_API_KEY`** — **Ben has provided this to Div.** Put it in **prod env only** (never DB/git); no OAuth (no consumer secret). The feature stays INERT until the key is set (no external calls, clean status message).
- **n8n cron:** schedule **`POST /api/tracking/poll`** (header `x-webhook-secret: <N8N_WEBHOOK_SECRET>`) every 1-2h. In-app timer is off on Vercel. Poller self-throttles (≤1 req/sec, 200/run).

### 5. Standing items (from the running queue — unchanged)
- **Signed-cookie auth-gate mirror** — share the cookie name/verification so the local gate matches prod (ports merge cleanly).
- **n8n Cin7 3PL-orders flow** — once live, the in-app "Import Cin7 orders" (+ `POST /api/supply/tpl/cin7-import`) can be retired (keep Map-to-Cost-Centres + Clean-up). Spec: `Claude Analyses/SPEC_N8N_CIN7_3PL_ORDERS_2026-08-21.md`.
- **Drop `sku_labels`** — live table, last write 22-Jun-26 (n8n stopped); confirm then drop.
- **Legacy Vercel cron** `59 23 * * 4` `/api/cron/action-metrics` in `vercel.json` — redundant with the n8n Thursday job; remove/retime.
- **polybags → Airtable/n8n** wiring + resync durability.
- **Live portal `hz-theme.css`** — confirm the portal `<head>` loads it (root cause of the old white-button bug; v27.668 has a fallback).
- **Stale GitHub PR #2** (v27.573-582 title) — close/retitle; you pull branch HEAD anyway.
- **Earlier storage batch** (v27.695) — same bucket/env as §2.

---

## What is blocked on BEN (so you know the dependency chain)
- Sandbox `SUPABASE_SERVICE_ROLE_KEY` (unblocks §2 testing).
- Fulfil go-live date + prod Fulfil ids sign-off (§3).
- Coghlans SFTP password (unrelated to this batch).
- (DHL API key — already provided to Div.)

---

## Per-version changelog (v27.742 → v27.751)

- **v27.742** — Order-plan **"not required in Cin7"** tickbox (CLIENT). Persists `purchase_orders.cin7_not_required` (mig 283, prior batch); greys the grid Cin7 cell to n/a. Fulfil unaffected (every PO still required in Fulfil).
- **v27.743** — **Two explicit ERP push buttons** (Cin7 / Fulfil), deterministic target (CLIENT). `erpUploadInert(po, forceFull, erp)` forces `?erp=<erp>`, fixing the sandbox bug where a "push to Cin7" silently followed Active-ERP (=fulfil). Fulfil pop is lines-only. (Ben chose two-button; no auto dual-write.)
- **v27.744** — PRODUCT P4a: `product_dev_samples.short_code` (**mig 284**) + `GET /api/product/scan/:code` (admin) & `GET /api/portal/scan/:code` (portal, ownership-guarded) + `GET /api/portal/product-swatch/:ref`.
- **v27.745** — PRODUCT P4b: QR + short code on the sample-card PDF (admin + portal). New encoder `lib/qrcode.mjs` (no CDN). QR encodes the portal scan URL.
- **v27.746** — PRODUCT P4c (admin): route `#/product/scan/<code>` → mobile phone sample card (swatch, Mark received, per-component decision/feedback, camera Add photo). Reuses `/received`, `/aspect`, `/photo`.
- **v27.747** — PRODUCT P4c (portal): supplier phone sample card (view: swatch + identity + receipt + D&B review read-only + Open full record). Ownership-guarded.
- **v27.748** — PRODUCT P4d: barcode scanner recognises a sample QR / typed 3-char code → opens the phone card; per-user "auto-mark received on scan" toggle (localStorage, OFF by default, never un-receives).
- **v27.749** — PRODUCT **P5** (**mig 285**, SERVER + CLIENT): dropped the 5 moved `product_dev_items` columns; every reader derives from `product_dev_requests`; item supplier is an aggregate of request suppliers; `portalOwnsProduct` + escalate + candidate-scoping re-pointed to requests; portal master drops Supplier/Recipient/Approval rows. **Deploy code before mig 285.**
- **v27.750** — PRODUCT **P6** (**mig 286**, SERVER + CLIENT): Timeline compose `/s` chip → tag a sample version (🧪 v_N_ token) → note linked via `supplier_notes.sample_id`, surfaces on that sample's review thread. `POST /api/product/note` accepts `sample_id`; `productSampleList` feedback_notes matches `sample_id` OR the legacy prefix. Additive slash mode — other compose surfaces untouched.
- **v27.751** — Fulfil ERP: **`POST /api/supply/fulfil/seed-ids`** — resolves suppliers → `party.party` and branches → `stock.location` by exact name against a chosen `env`, upserts `suppliers.fulfil_id` / `branches.fulfil_id` (dry-run default; `apply:true` writes; only matched rows written). See §3 for the go-live run.
