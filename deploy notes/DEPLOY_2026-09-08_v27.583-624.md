# Deploy note for Diviyaj — HORIZON v27.583 → v27.634

**Live now:** v27.624 (deployed 08-Sep 14:53, mig 270 applied) · **This package:** v27.583 → v27.634 · Branch `phase-2.1-suppliers`
**No new migrations since 270. No new npm deps since ssh2-sftp-client. No new env vars.** All of 625→634 is `artifact_v16.7.html` / `supply/inject.html` / `supply/hz-theme.css` only.

> **v27.625–634 (added after v27.624 went live):**
> - **625** Performance v2 "Our actions · by category" card (Supply metrics = two-column canvas: sticky dark scoreboard + by-category card with composition bar, 5-bar history, delta chips, PO-list foot expander; Waiting/Opportunities kept). Same action-metrics data.
> - **626** Buy-plan popup: strong-red cells (`clo-dk`/`red-dk`) were red-on-red (unreadable) → white text.
> - **627** Inventory v2 panels dark-mode fixes (FBA-aged pills, 3PL cards/toggle white→tokens).
> - **628** Buy v2 slice 1: grid-card frame + buy-popup selected-cell contrast (dark-red on light selection).
> - **629** App-wide token sweep — 137 residual hex → design tokens across all renderers (dark-mode); drawing/SVG untouched.
> - **630** Buy v2 slice 1b: filter toolbar as a card. buyplanItems byte-identical.
> - **631** Demand plan: target-rec **overall inheritance** (categories inheriting the all-categories % now get recommendations); Variant image → top of Display Settings + 📷; col-1 width +30% (160→208).
> - **632** Demand plan SKU filter no longer persists / independent of buy & FBA.
> - **633** Create Production PO's: "↻ alt recommendation" filter tickbox (+ generalised the ↻ now N hint).
> - **634** Smoothing guard: pure **set** sub-categories (e.g. MIXNMATCH) can no longer inflate the forecast (interim; two-totals proper fix still to build). buyplanItems byte-identical.
>
> All render-checked (0 JS errors); the buy-plan output is unchanged across 625→634 (verified byte-identical). Cosmetic + two demand-plan logic fixes (target-rec inheritance, smoothing guard) — neither moves the default buy. You port + deploy.

## Must-do on live
1. **Run migration `migrations/270_buy_projects.sql`** — new table `planner.buy_projects` (id, name, mode, data jsonb, created_by, created_at, updated_at + unique index on lower(name)). Backs the Create-PO "Save Project" feature. Additive; nothing else depends on it.
2. **`npm install`** — one new dependency: `ssh2-sftp-client@^12.1.1` (already in package.json), for the new Coghlans SFTP browser.

## New env vars (OPTIONAL — SFTP feature stays inert without them)
Only needed to switch on CONFIG ▸ Coghlans SFTP. Until `COGHLANS_SFTP_PASSWORD` is set the page shows "not configured" and makes no connection — safe to deploy without.
```
COGHLANS_SFTP_HOST=152.67.109.140
COGHLANS_SFTP_PORT=22
COGHLANS_SFTP_USER=dok
COGHLANS_SFTP_PASSWORD=            # Ben to supply
WEBSHARE_PROXY_HOST=104.165.164.165
WEBSHARE_PROXY_PORT=80             # confirm with Ben
WEBSHARE_PROXY_USER=
WEBSHARE_PROXY_PASSWORD=
```
New server file: `coghlans_sftp.mjs` (read-only SFTP-over-HTTP-CONNECT browser). Endpoint `GET /api/config/coghlans-sftp/list` (config-level auth).

## What changed (grouped)

**Buy-affecting — review these (buy numbers move, all downward corrections):**
- v27.588 / 594 / 595 / 597 — SET over-explosion fixes. Set % is a share of *exploded* units; active-set and run-off/discontinued saved overrides no longer multiply out by set size. Net effect on live is a **reduction** in over-stated set-component buys (Ben has the impacted-SKU list). No engine-logic change beyond the ÷setSize correction; a DEMAND_BUILT timing guard stops a race that briefly showed inflated set buys.

**Create Production PO's — full redesign (v27.605–613)** — the "Create Purchase Orders" button on #/buy-move/buy is now a full-screen page (not a modal) that never auto-reloads: season badges, carton-qty note + non-carton-multiple warning, China-stock column, CA excluded by default (opt-in), in-production visibility in Total, **Save Project** (needs mig 270), add-to-existing-PO, shortened tier-top-up tooltip.

**UI design uplift (Diviyaj's "Premium Polish" canvas) — cosmetic, no data/logic change:**
- Dark mode foundation + moon toggle (v27.584/585/590/591); header P1b — gradient account avatar + account menu, SVG icons (v27.616).
- DEMAND: Actions cards + DTC Mismatch stat chips (v27.617), plan grid-card frame (v27.618), sticky column/border fixes (v27.601/614).
- Performance v2: Demand-metrics in-stock KPI cards + rate table (v27.619), Supply-metrics open-actions scoreboard (v27.620) + supply-pipeline flow card (v27.621).
- Buy & Move v2: Actions rebuilt as a KPI strip + action×country matrix (v27.622) — display-only, reads the same buy numbers.
- Reports v2: Exec Summary hero cards + framed matrix (v27.623).
- Inventory: market pills tokenised, dark-mode fix (v27.624).

**Fixes / quick-wins (no migration):**
- Contribution model: clobber + channel-scope guard (v27.586); 2027 "last year" column uses the actual, not the stale prior-year forecast (v27.587).
- Summary & targets: figure cells + tooltips follow the currency picker (v27.599); "Apply everywhere" popup opens again (v27.600).
- Payments Due: PAY optimistically drops the row from Outstanding (v27.602).
- Auto-updater: DATA-only changes no longer reload a backgrounded tab — fixes the Buy page "constantly refreshing on idle" (v27.603).
- Price Lists: tier/price edits refresh silently, no "Loading…" flash (v27.598).
- Xero Compare: flags the invoice-TOTAL mismatch, not just the due amount (v27.593).
- 3PL Invoicing ▸ Forecast: weekly EU-iFulfilment view + savable B2B units/order (v27.592).
- Demand Metrics: historical point-in-time via an As-of month picker (v27.611/615).

## Notes
- No other schema changes in this range (270 is the only migration).
- Everything render-checked (0 JS errors); Ben's browser sign-off still pending on the UI batch.
- Cin7 SalesOrder field updates done this session were direct live API writes (invoice/tracking on AU orders) — operational, not part of this code deploy.
