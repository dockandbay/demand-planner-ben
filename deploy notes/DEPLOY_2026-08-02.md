# Deploy note — 2026-08-02 (for Diviyaj)

**Live baseline:** v26.299 (your last merge; migrations 159/160/161 applied).
**This package:** v26.300 → **v26.401**.

⚠️ **This SUPERSEDES the 2026-07-31 note** — that bundle (v26.300→345) wasn't pushed on Friday, so it's rolled into this one. Everything below is a single consolidated deploy. Full per-version detail in `CHANGES.md`.

---

## ⚠️ Migrations to apply (8): 162 → 169
All additive / `IF NOT EXISTS`, safe to re-run, apply in order:

| Mig | Adds | For |
|---|---|---|
| **162**_suggestion_stakeholders | `suggestions.stakeholders text` | per-suggestion stakeholder emails (SUG-0003) |
| **163**_branch_fulfil_id | `branches.fulfil_id text` (**schema only — do NOT seed ids**; entered per-env in CONFIG ▸ Branches) | Fulfil warehouse mapping |
| **164**_payment_remittance | remittance columns/table | Payments Report remittance upload (v26.348) |
| **165**_shipment_change_log | `shipment_change_log` | shipment record-of-change (v26.362) |
| **166**_product_recipient_countries | recipient-country column | Product dev recipient country, SUG-0005 (v26.365) |
| **167**_sample_second_recipient | 2nd recipient + tracking cols | sample shipment 2nd recipient (v26.366) |
| **168**_demand_revenue_targets | `demand_revenue_targets` table | DEMAND ▸ Revenue growth targets (v26.368) |
| **169**_price_changes | `price_changes` table | DEMAND ▸ Revenue price changes (v26.368) |

**No new npm dependencies.** (Run `npm install` anyway — harmless.)

---

## New env vars — OPTIONAL (Fulfil, gated OFF)
Unchanged from the 07-31 note. Nothing breaks without them (no keys → caught `NO_FULFIL_CFG`, nothing sends). Active ERP still defaults to `cin7`; `FULFIL_LINES_SEND=false` hard-coded (dry-run resolution only).
```
FULFIL_SANDBOX_SUBDOMAIN=   FULFIL_SANDBOX_API_KEY=
FULFIL_LIVE_SUBDOMAIN=      FULFIL_LIVE_API_KEY=
```

---

## What's in the bundle

### A. Carried over from the un-pushed 07-31 note (v26.300–345)
- **Suggestion box + supply/portal fixes** (300–321): SUG-0003 stakeholders (mig 162), SUG-0011/0012/0013, PO-grid tidy, deposits/payments-due fixes, supplier add/portal-user fixes.
- **Fulfil ERP integration — GATED, safe** (322–338): mirrors Cin7 "Update ERP", targets Fulfil only when switched; all safety gates in place (see table above + 07-31 note). Blocker for real use = Fulfil sandbox catalog import (ops task, not a deploy blocker).
- **Demand plan visual overhaul** (325–345, artifact only): colours, dividers, FY columns, category-header cleanup, padding.

### B. New since Friday (v26.346–401)
- **Payments/CONFIG** (346–350): FY show/hide badge; remittance upload + delayed paid-email (**mig 164**); Permissions table width; CONFIG nav Admin group + Products under Supply chain.
- **Record-of-change** (351–363): PO invoice/order-plan edits + line delete; dd-mmm-yy dates + money 2dp; shipment record-of-change (**mig 165**); hide-record-of-change persists per-PO/shipment.
- **Supplier management** (355–359): manage-supplier drawer, inline portal users, magic-link per user, cell-picker pins selected value.
- **Order plan** (360, 381–387): near-instant edit/add/delete; paged PO columns (20 + Load-more, scroll preserved); default to latest in-production PROD#; per-PO "Accept all" with itemised tooltip; supplier-changes banner one-row layout. **✅ Ben-verified 2026-08-02.**
- **Product dev / samples** (365–366): recipient country (**mig 166**), sample 2nd recipient + tracking (**mig 167**).
- **DEMAND ▸ Revenue tab** (367–373): growth targets + tracking + price changes (**migs 168/169**, DEMAND-scoped write guard); monthly breakdown + country subtotals; per-FY toggle; Revenue-vs-target pop-out; L3 sub-nav render fix.
- **Performance** (375–379): parallelised slow endpoints + memoised `calc()`; BI/KPI compute cache; lean PO picker; scoped admin portal-preview fetch. (gzip already on.)
- **PO archiving** (380): CONFIG ▸ Admin ▸ General, production-number cutoff hides completed POs from grid + Order Plan (never portal/cashflow); app_settings key `po_archive_before_prod`.
- **DEMAND plan** (388–397): forecast-cell edit fixes (stale calc memo, grand-total + category-subtotal live update); 1h filter persistence (plan + Buy/FBA); green SKU-filter fields; report buttons in filters row + **Executive Summary drawer**.
- **⚠️ BUY ENGINE — changes real order quantities** (393–395, 398): fixed `fwdDemand` off-by-one (target cover excluded the arrival month; systemic buy uplift); urgent now bridges to next order with a cover buffer; future-buy `+Nf` flag; **FBA transfer unified** so the buy plan's 3PL→FBA transfer = the Transfer FBA tab (carton-rounded, hybrid 50%-cap + 4wk-3PL floor), and it's used in the buy math. Impact snapshotted (buy 3PL +0.2% overall). *These are the only changes here that move buy numbers — flagged for awareness; Ben is verifying.*
- **Manufacturing tab** (399): grouped by production; component supply split In-production / Completed (completed now counts); ready-to-ship counts as at-factory. *(Fast-follows pending: per-production allocation + manual stock-cover — not in this bundle.)*
- **Forecast reports** (400–401, read-only, no buy-plan impact): Auto-Forecast pinned to latest `run_id` (stops silent multiply if a 2nd forecast run lands), surfaces overdue units, ref includes year; Exec Summary + Revenue tab now mirror the plan (raw forecast, full-month forecast for the current partial month).

---

## FYI (no action needed)
- **Live data already changed by Ben (not code):** SUG-0005/0008/0011/0012/0013/0014 set to status `ready to deploy` directly in `planner.suggestions` (they're the built-but-unshipped ones). This deploy ships their code.
- **Not deployable yet / excluded:** Fulfil end-to-end (needs sandbox catalog import); manufacturing fast-follows.

## Files changed
`server.mjs`, `supply/inject.html`, `artifact_v16.7.html`, `migrations/162…169`.
