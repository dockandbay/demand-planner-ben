# DEPLOY — HORIZON v26.828 → v26.861 (2026-08-11)

**Live baseline:** v26.827 · **This package:** v26.861

## TL;DR for Diviyaj
- **Code only + 1 migration.** No new npm dependencies, no new env vars.
- **Migration to run on prod:** `migrations/208_xero_compare_file.sql` (adds one nullable column — safe, idempotent `ADD COLUMN IF NOT EXISTS`).
- Files changed: `artifact_v16.7.html`, `server.mjs`, `supply/inject.html`, `supply/portal-view.js`, `package.json`.

## Migration
| # | File | What it does | Risk |
|---|------|--------------|------|
| 208 | `208_xero_compare_file.sql` | `planner.xero_compare_snapshot ADD COLUMN IF NOT EXISTS file_b64 text` — stores the raw uploaded Xero XLSX so the report can offer it back for download | None (nullable add) |

(Migrations 204–207 were already in the v26.827 baseline — nothing to do there.)

## Data already applied directly to LIVE (no action needed — logged for awareness)
These were applied to prod (`oolwklahstnvocaugryg`) directly via MCP as guarded, changed-only updates — they're already live, listed only so you know the state:
- `planner.products` FBA availability (5 cols: available_{us,uk,au,eu,ca}_fba) — 227 rows
- `inventory_us_awd` — 91 rows; non-GRS (`inventory_us_nongrs`, `inventory_uk_nongrs`) — 115 rows
- `price_changes` — EU price increase, effective **2026-09**, all channels (Beach Towel split 10/7/7 across core/designed/summer) — 21 rows
- `discontinue_date_final` / `discontinue_date_au_final` — 217 rows (0 clears; `discontinue_date_ca` left untouched)
- Shipping-bag (SHIPBAG) EU-DTC forecast override — 54 rows

## Post-deploy action ⚠
- **Remove the LIVE visibility-seed `sales_actuals` row** that was added so pre-launch/availability-only SKUs would render before v26.833 shipped. v26.833 (availability-driven subcategory rows) is in THIS package, so once deployed the seed row is no longer needed — delete it.

## What's in this package (v26.828 → v26.861)

**DEMAND plan**
- v861 Key Accounts: `+ add SKU` dropdown flips **up** at page bottom (was clipped)
- v855–860 Select cells → running **Sum** box (category + SKU rows; ⌘/Ctrl-hover to paint, plain click clears, pinned far-right of the toolbar)
- v835/854/856 Key Accounts `add SKU` = ACTIVE-only multi-add search (fixed clipping + dead dropdown)
- v834 `⬆ Import forecast` — SKU-level manual override upload
- v833 Subcategory rows driven by **availability**, not just sales history *(triggers the seed-row cleanup above)*
- v828 Currency pill shows on the plan + converts revenue; removed redundant Category-totals chip
- v829/830/838 Variant-image thumbnail on SKU rows + colour-swatch fallback
- v831/832 Category within-group order follows curated list; record-of-change shows R pre-flush
- v837 SKU `est ▾` anomaly tooltip now matches the popup + apply records the change
- v836 BUY plan: inbound PO links in the plan popup open the PO drawer (non-links no longer styled blue)

**PAYMENTS / Xero Compare**
- v839–843/847 Narrower columns, re-downloadable XLSX, clearer headers ("Xero Due (GBP)/(USD)"), ignore SUPPLIER-PAYMENT batches, match Other Payments, fix deposit-ref "PO not found", **Horizon Due = final invoice − payments on/before the report period-end date**
- v845 Instant PAY button + cached deposits fetch (perf)
- v846 Likely pay date appears in the payment lists immediately

**SUPPLIER PORTAL / SAMPLES / PRODUCT**
- v844 De-dup the "submitted invoice info" email (only re-sends if amount changes)
- v848 PRODUCT recipient country adds US + Direct to Client
- v849/850 Portal sample-shipments grid ("Dock & Bay Status" + "Your status"), new-product cache refresh, "Set shipping" rebinds the card, supplier SRs show immediately
- v851 PRODUCT sample refs open an editable drawer (no page jump)
- v852/853 Samples: "created" timeline entry, always-editable/autosave details, tighter layout, white input boxes

## Rollback
Pure code + one additive column. Revert to v26.827 commit; column 208 can stay (unused) or be dropped.
