# DEPLOY 2026-08-24 — HORIZON (for Diviyaj)

**Package:** last note ended **v27.247** → now **v27.269** · Branch: `phase-2.1-suppliers` · From: Ben

**Migration: `243_launch_ramp.sql` (buy_complex_rules.ramp_months, ramp_sl). No new env vars. No new dependencies.** Standard pull + Vercel deploy. Files touched: `server.mjs`, `artifact_v16.7.html`, `supply/inject.html`, `package.json`, `CHANGES.md`, docs.

---

## ⭐ ACTION NEEDED — refresh the live data cache

Ben ABC-tiered the SS27 products (their `market_tier` is now correct in `planner.products` — verified 149/149), but they weren't showing in the DEMAND tier filter on live. **Root cause: the live data cache (`horizon:data` blob) was stale** — it only rebuilt on an explicit invalidate, so the ETL'd tier changes never surfaced.

**Please refresh it now** (one of):
- `POST /api/data-cache/invalidate` with the `x-webhook-secret: <N8N_WEBHOOK_SECRET>` header, **or**
- any forecast save on live (also triggers the rebuild).

After that, SS27 + Tier A will show (with "product live dates = ALL", since SS27 launches in 2027).

**Permanent fix shipped (v27.257):** the blob now **self-rebuilds from Supabase whenever it's >6h old** (`MAX_BLOB_AGE_MS`), and boot rebuilds a stale blob — so live data can't sit stale >6h without any manual step. **Recommended:** wire n8n to `POST /api/data-cache/invalidate` at the end of its daily product/data sync, so ETL changes show *instantly* instead of within 6h. (The invalidate endpoint already exists + is secret-gated.)

---

## What's in this package (v27.248-269)

- **v27.269 SSM Backtest: widen Recommendation column + tighten metric columns** — fixed table layout/colgroup; Recommendation doubled, the 5 metric columns ~30% narrower. Client-only.

- **v27.268 SSM Backtest: fix cut-off Recommendation column** — risk-options cell now wraps (was nowrap → clipped). Client-only.

- **v27.267 Inventory Status Report: next-shipment PO + FBA inbound + PO hyperlinks (SUG-0031/0032)** — inbound column shows the next shipment PO (linked to the PO drawer); FBA rows show FBA-specific inbound + on-order; snapshot-upload form clears on success. Client-only (`artifact_v16.7.html`).

- **v27.266 Buy plan: Launch/NPD ramp Complex Rule (SSM service-level bump)** — new `launch_ramp` coverage type: elevated SSM service level for N months post-launch, decaying to tier SL (per-month floor). Rule editor + server + **migration 243** (`ramp_months`, `ramp_sl`). Regression-safe (inert when no such rule). NOTE: SSM maths are now-anchored → ramp/SSM-safety inert for launches >~6mo out until closer (launch-anchored SSM is the follow-up).

- **v27.265 Buy plan: windowed range Complex Rule now SSM-sized** — the up-front block buy is sized by SSM (season-detected coverage + tier-aware safety stock = Z·√(lead·σ_d²+demand²·σ_lead²)), not raw range demand. New helper `ssmSafetyUnits()`; `ssmCoverWeeks` untouched. Verified: only SS27 SKUs changed, non-SS27 byte-identical. Client-only.

- **v27.264 Buy plan: windowed "range" Complex Rule = up-front block buy** — a range rule with a window now places one up-front order at the window start for the full range demand (net of stock/on-order), instead of per-month cover raises that a long lead pushed into the past (→ late buy). Verified: only SS27 SKUs affected, non-SS27 byte-identical. Client-only (`artifact_v16.7.html`). Depends on `planner.buy_complex_rules` (mig 170) being live.
- **v27.263 3PL invoice: auto-to-Other for ALL 3PLs + "could not reconcile" warning box** — unmapped freight auto-books to each 3PL's Other account (iFulfilment/Coghlans → Other Costs; Geneva → Geneva Other fees; ILG → Other Fees); new amber warning box lists the amount + exact unreconciled references. Unified `autoOther` signal. Server + client.
- **v27.262 3PL invoice (UK ILG): unmapped freight auto-books to Other Fees** — freight with no Cin7 cost centre now lands in "Other Fees" (303.33) so the bill balances (no "COST CENTRE MISSING" gap); the Clean-up sweep is still offered to split it by channel. ILG-only. Server + client.
- **v27.261 3PL invoice: clear 5-step workflow** — numbered steps (Upload → Import Cin7 → Analyse → Clean-up sweep → Review/download); removed the confusing per-file Analyse button (now a dedicated step-3 button that auto-picks the data file); sweep auto re-analyses. Client-only (`artifact_v16.7.html`).
- **v27.260 3PL invoice (UK ILG): combine all freight files + fix Clean-up sweep** — ILG allocation reads all `invoice_0*` freight files combined; returns an `unmapped` object so the Clean-up sweep button/message show when freight is unmapped; sweep endpoint extracts ILG refs via the ship-detail parser so it actually fetches cross-month orders. Server-only (`server.mjs`); other 3PLs unchanged.
- **v27.259 3PL invoice reconcile: VAT gross-up (display only)** — the reconcile box now has an editable "+ VAT %" field (default 20) and reconciles the gross PDF total against net×(1+VAT). Client-only (`artifact_v16.7.html`); the Xero bill/CSV export stays **net** (Xero adds VAT via line tax type). Pairs with v27.258 so July now reads "matches".
- **v27.258 3PL invoice (EU iFulfilment): book Consumables Cost at order level** — iFulfilment bills a separate "Consumables Cost" column that sits OUTSIDE "Total Excl Shipping" / "Total Cost", so Horizon never booked it (~€942.74/mo, ~€11k/yr dropped). Confirmed vs INV-192570 (Jul-26): net €48,259.51 = app-booked €47,316.77 + Consumables €942.74; ×1.2 VAT = €57,911.41. Fix folds the column into each order's fulfilment (→ Xero bill, per cost centre) + into the reconcile total. Exact-match + order-sheet-only → iFulfilment only; Geneva/ILG/Coghlans byte-unchanged. **After deploy:** re-run July (and worth checking prior months) in REPORTS ▸ 3PL Invoice ▸ EU iFulfilment so past bills pick up consumables. NB the reconcile total is NET; the invoice PDF is GROSS (Xero adds 20% via line tax type) — comparing to the PDF still needs ×1.2 (a separate open display item, not yet built).
- **v27.257 Live data cache self-heal** — see above. `bgRefresh` now rebuilds from Supabase past a 6h max-age (was re-read-only). Audit note: all other server caches (makeCache PO/order-plan/lookups/exceptions, section response-cache, portal, open-actions, actions, trends) already rebuild from source on their 5-10min TTL — the `horizon:data` blob was the only one with the indefinite-staleness gap.
- **v27.256 Price-editor z-index** — the "from production" picker popup opened behind the SET PRICE modal; fixed.
- **v27.255 / 27.253 Price editor "copy tiers from another SKU"** — search a SKU, select it, choose "apply to" (current price OR "+ new future price from prod N" — adds a future price point), click Apply. + XLSX Summary: category left-aligned + a **Tier** column (`market_tier`).
- **v27.254 / 27.252 Create-PO tier-savings hint** — persistent bright-yellow box next to MOQ: "N units cost $X/unit. The extra M units to reach T ($Y/unit) cost $Z — $/unit"; free/negative-marginal case called out.
- **v27.251 / 27.250 Create-PO XLSX export** — after Preview, **⬇ Download XLSX**: "Summary" tab (SKUs by category × country, country columns centred + coloured to MKT_COLORS, Tier column) + "Purchase Orders" tab (POs by supplier, SKU · Qty · Product, PO total under SKU). New endpoint `POST /api/supply/buyplan-pos/export.xlsx` (exceljs).
- **v27.250 Create-PO MOQ from price-list tiers + 3PL Cin7 import per-3PL coverage fix** — MOQ = lowest tier min-qty for the chosen supplier/production; and the 3PL Cin7 import's incremental "already imported" check is now scoped **per-3PL** (was period-only, so one 3PL's import wrongly blocked the others).
- **v27.249 Mobile nav** — L2 sub-nav moved into the hamburger for all top views (incl. REPORTS/PRODUCT expand + Admin in CONFIG); on-page L2 rows hidden on phone. Desktop unchanged.
- **v27.248 3PL invoice upload gzip** — the file is gzipped in the browser before the base64 POST (server gunzips before storing), so CSV invoices up to ~11-15MB clear Vercel's ~4.5MB body cap. (.xlsx is already compressed → still capped; message says export CSV.)

---

## FYI — live Cin7 data ops done this session (with Ben's approval)

Not code — direct live operations, logged here for your awareness:

1. **Cin7 InvoiceDate backfill (EU iFulfilment, branch 25073).** Many dispatched EU orders had `stage='Dispatched'` but a **null `invoiceDate`**, so the InvoiceDate-based 3PL import missed them (only 145 of ~2,500 July orders imported). Set `invoiceDate = dispatchedDate` where dispatched + null, per month: **June 600 · July 2,105 · Aug ~1,657** (May & earlier were clean). Backup files of affected order-ids saved under `temp/` (gitignored). This is a **source-data issue in Cin7** — worth fixing at source (why don't dispatched EU orders get an invoiceDate?) or via a **daily n8n backfill** running the same logic, since Aug (live month) keeps drifting.
2. **Re-imported EU iFulfilment July** into live `planner.tpl_cin7_orders` (via the app's Sweep) → now 2,507 orders (was 145).

## Outstanding n8n asks for Diviyaj
- **Data-cache invalidate** post product-sync (above).
- **Cin7 → `tpl_cin7_orders` daily import** — spec: `Claude Analyses/SPEC_N8N_CIN7_3PL_ORDERS_2026-08-21.md` (once live, Ben removes the in-app "Import Cin7 orders" step, keeps Clean-up).
- **Cin7 dispatched-invoiceDate backfill** (daily, or fix at source) — see FYI #1.
- Migration 242 (`report_notes`) from the last note is unused — skip if not run.

## Deploy
Pull `phase-2.1-suppliers` through **v27.269**, deploy on Vercel, run **migration 243_launch_ramp.sql**, then **refresh the data cache** (top of this note). No env / dependency changes.
