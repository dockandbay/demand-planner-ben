# Deploy note — v27.756 → v27.776 (18-Sep-2026)

Covers the **supplier "accept" workflow** (mig 287), the **SAMPLING sample-lifecycle colour model + silent receive + per-sample tracking**, the **grid grouping toggle** (SAMPLING + PRODUCTS), **SAMPLING ▸ Reports**, the **Fulfil push finish** (FOB + dates + price), and the Portal ▸ Productions approval strip. Continues from `DEPLOY_2026-09-17_v27.742-750.md` (through v27.755, migs 284-286).

---

## AT A GLANCE

**Repo/branch:** `dockandbay/demand-planner-ben` · `phase-2.1-suppliers` · HEAD **v27.776** (pushed, `79d5ea1f`). Live = v27.694 (unchanged since last note — this whole batch is still to deploy).

**Migrations to apply — ONE, additive, safe any time:**
- **287** `planner.product_dev_requests.{supplier_accepted_at timestamptz, supplier_accepted_by text}` — the supplier "accept" workflow. `ADD COLUMN IF NOT EXISTS`, rollback-safe, no backfill. Mirrors the PO-confirm pattern (plain acknowledgement, does not gate sample submission).

No destructive migration this batch (unlike 285 last time).

**Env vars:** **none new.** `DHL_API_KEY`, `N8N_WEBHOOK_SECRET`, `FULFIL_*`, `SUPABASE_*`, `STORAGE_MAX_MB`, `PORTAL_URL` all as per the prior note (Open Items below are unchanged except Fulfil, which has moved forward — see §2).

**Config toggle (no migration — uses the existing `app_settings` key/value table):**
- **`sampling_group_mode`** ∈ `inline` (default) | `tabs` | `tree`. Set in CONFIG ▸ Product; team-wide; drives the grouping layout of **both** the SAMPLING grid and the PRODUCTS grid. Saved via `POST /api/app-settings {key,value}`. Nothing to provision — the row is created on first save.
- **DHL auto "Package received"** — a toggle in CONFIG ▸ Admin ▸ DHL tracking (`auto_package_received`). When on, a sample whose DHL tracking reads *delivered* auto-advances to SAMPLE PACKAGE RECEIVED. Stored in the tracking config, no env, no migration.

**New routes:**
- `POST /api/portal/product-accept` (portal, ownership-guarded) — supplier accepts a product's development assignment. Whole-product grain: sets `supplier_accepted_at/_by` on every request that supplier holds for the item; writes a timeline entry **and** a record-of-change row.
- `GET /api/product/reports/sampling` (admin) — powers SAMPLING ▸ Reports (metrics, type×season pivot, supplier KPIs, detail popout, PDF).

**New deps:** none. **Files touched:** `server.mjs`, `supply/inject.html`, `supply/portal-view.js`, `supply/portal.html`, `supply/hz-theme.css`, `migrations/287_dev_request_accepted.sql`, `vercel.json`, `package.json`, `CHANGES.md`.

**`vercel.json`:** `lib/**` added to `includeFiles` and the n8n webhook-gate exemptions — these mirror hotfixes Diviyaj already applied on prod (v27.756). No new action; listed for completeness.

**Post-deploy smoke:**
1. `/api/version` = **v27.776**.
2. PRODUCT ▸ SAMPLING renders. Swatch column hugs the swatch (no wide left gutter), the reference sits on one row, and column 3 shows one colour (bulk, else development) with the component + category beneath.
3. CONFIG ▸ Product ▸ grid layout: switch **inline → season tabs → tree**; both SAMPLING and PRODUCTS grids follow, for everyone.
4. A sample tick shows the lifecycle colour (dev = blue, shipped = orange, package-received = light purple, in-review = dark purple, approved = green, rejected = dark red, stop = bright red); the legend key renders. Click **Receive** on a sample — it updates **silently** (no full-page reload) and moves the sample to SAMPLE IN REVIEW.
5. Portal (as a flagged supplier): a new-development product shows an **open "Accept" action** until accepted; accepting clears it, and the admin SAMPLING grid + request detail show the accepted state; the timeline and record-of-change both log it.
6. SAMPLING ▸ Reports: big metrics (products in progress, samples in progress), the type×season pivot, and the supplier KPI table render; open a supplier's detail popout and download its PDF.
7. Fulfil push (sandbox or live per CONFIG ▸ Active-ERP): a pushed PO carries **incoterm FOB**, `requested_shipping_date` = production end date, `requested_delivery_date` = estimated completion (final delivery to warehouse), and a **unit price** (e.g. 4.18) sent as a decimal string.

---

## ⭐ OPEN ITEMS FOR DIVIYAJ (please assist)

Carried forward from the prior note; **§2 (Fulfil) has advanced** — read it, the rest is unchanged.

### 1. Deploy this batch + migration
- Deploy through **v27.776**; run mig **287** (additive, any time).
- No env changes required for the features in this batch.

### 2. Fulfil ERP go-live — NOW LIVE-VERIFIED IN DEV
Ben flipped **`app_settings.fulfil_env = 'live'`** in the dev/sandbox app and **ran a real live PO push — it succeeded** (create + lines + header). Two things were fixed while testing and are in this batch:
- **Price now pushes** (v27.764). The unit price is sent as a **decimal string** (`String(Math.round(price*10000)/10000)`); sending it as a JSON float made Fulfil reject it ("Unit Price exceeds digits limit of 5" — a full-precision `Decimal('4.20000…')`). Verified pushing 4.18.
- **Header fields** (v27.764): **incoterm `FOB`** (uppercase), `requested_shipping_date` = the PO's **production end date**, `requested_delivery_date` = the **estimated/calculated completion date** (final delivery to warehouse). ⚠ `requested_delivery_date` is **the field to compare between the Horizon PO and Fulfil** for drift.
- Warehouse = China Port (`CHP`) and the `final_destination` metafield are as per the prior note (v27.754); v27.760 additionally writes the final destination into the **PO comment** (Ben: metafield mandatory, comment fine too).

**Still required to take it live in prod (unchanged from prior note):**
- Set prod env `FULFIL_LIVE_SUBDOMAIN` + `FULFIL_LIVE_API_KEY`.
- Run **`POST /api/supply/fulfil/seed-ids?env=live&apply=true`** once in prod (dry-run first) — sandbox party/warehouse ids must NOT be carried over.
- **[Diviyaj]** Run the **Fulfil PO import** into `planner.fulfil_purchase_orders` (mig 282) so drift compare has data. Ben could not populate it: the prod mirror is only reachable through the read-only `planner_ro` role, which also lacks SELECT on that table — so this is yours. Wire the n8n cron to `POST /api/supply/fulfil/import-pos` (header `x-webhook-secret: <N8N_WEBHOOK_SECRET>`); the in-app 6h timer is guarded off on Vercel.
- **[needs Ben]** confirm the go-live date / the parallel-with-Cin7 window.

### 3. DHL tracking go-live — unchanged
- `DHL_API_KEY` in prod env only (Ben already gave it to Div). Feature stays inert until set.
- n8n cron `POST /api/tracking/poll` (header `x-webhook-secret`) every 1-2h.
- New this batch: the **auto "Package received"** toggle (CONFIG ▸ Admin ▸ DHL tracking). When on, a *delivered* tracking status auto-advances the sample to SAMPLE PACKAGE RECEIVED. Off by default; no external calls beyond the existing poll.

### 4. Large-file uploads (Supabase Storage) — unchanged
Bucket `horizon-uploads`, `SUPABASE_URL` + `SUPABASE_SERVICE_ROLE_KEY` + `STORAGE_MAX_MB=100`, raise prod global Storage limit to 100MB, run mig 276 if not applied. Full spec: `DEPLOY_2026-09-16_v27.695_storage-uploads.md`.

### 5. Standing items — unchanged
Signed-cookie auth-gate mirror; n8n Cin7 3PL-orders flow (then retire in-app import); drop `sku_labels`; remove/retime the legacy Vercel Thursday cron in `vercel.json`; polybags → Airtable/n8n; confirm live portal loads `hz-theme.css`; close/retitle stale GitHub PR #2.

---

## What is blocked on BEN
- Fulfil go-live date + prod Fulfil-ids sign-off (§2).
- Sandbox `SUPABASE_SERVICE_ROLE_KEY` (unblocks storage testing).
- ERP deviations labelling decision (whether the compare UI is split into "Cin7 Compare" / "Fulfil Compare" tabs vs one toggle — Ben still to confirm).
- Coghlans SFTP password (unrelated to this batch).

---

## Per-version changelog (v27.756 → v27.776)

- **v27.756** — Mirror Diviyaj's prod hotfixes: n8n webhook-gate exemptions + `lib/**` in `vercel.json` includeFiles. Already on prod; no new action.
- **v27.757** — Portal ▸ Productions: PO approval strip (**Approve** / **Approve all**), per-batch "to approve" counts, "unapproved of total" menu badge.
- **v27.758** — PRODUCT ▸ SAMPLING design slice 2: state-coloured sample ticks + notes superscript; MANAGE panel styled as a board card.
- **v27.759** — SAMPLING design slice 3: type scale, skeleton loading, phone cards, MANAGE facts column.
- **v27.760** — Fulfil push: write the final destination to the PO **comment** as well as the metafield (Ben: metafield mandatory, comment fine too).
- **v27.761** — PRODUCT **supplier "accept" workflow** (**mig 287**, SERVER + CLIENT + PORTAL). Whole-product grain; `POST /api/portal/product-accept`; admin sees the accepted state on the SAMPLING grid + request detail; the supplier sees an open action until accepted; logged to the timeline **and** record of change. Plain acknowledgement — does not gate sample submission.
- **v27.762** — SAMPLING grid grouping (season → type, like the PRODUCT grid) + MANAGE tab-row header tidy + PRODUCTS grouping de-coloured/restyled.
- **v27.763** — SAMPLING grid grouping **layouts**: `inline` / season-tabs+type-pills / season-type-tree; toggle in **CONFIG ▸ Product** (team-wide `app_settings.sampling_group_mode`).
- **v27.764** — Fulfil push: **incoterm FOB** + `requested_shipping_date` (production end) + `requested_delivery_date` (estimated completion / final delivery to warehouse) + **unit price sent as a decimal string** (fixes the "digits limit of 5" rejection; price now pushes, verified 4.18).
- **v27.765** — Crossdock SKU picker: a SKU already on the PO shows as a disabled "already on this PO" row instead of "no matching SKU".
- **v27.766** — PO grid Direct cell: clamp to 2 lines + 120ms full-list tooltip; PRODUCTS grid shares the SAMPLING grouping toggle.
- **v27.767** — PRODUCTS mobile: drop the count badge on the PRODUCTS tab; the tabs/tree layout now filters the mobile cards by category.
- **v27.768** — SAMPLING/PRODUCT mobile cards: label on top (small, subtle) with the data full-width beneath (was a 40% left gutter).
- **v27.769** — SAMPLING: **sample lifecycle colour model** (7 states) + **silent receive** (no full-page reload) + full-width mobile timeline. RECEIVED now sets status SAMPLE IN REVIEW; adds SAMPLE IN TRANSIT and SAMPLE PACKAGE RECEIVED; an un-received slot is a dashed open circle before S1.
- **v27.770** — SAMPLING: per-sample **SR + tracking reference under each tick** (dynamic links); the standalone Shipment column dropped; SR opens the sample in the drawer with DHL detail; **DHL auto "Package received"** config toggle.
- **v27.771** — SAMPLING timeline: **optimistic** sample-tagging on an existing message + the ability to tag a sample on a **new** message (no lag, tagged in the UI immediately).
- **v27.772** — SAMPLING: the MANAGE review-panel SR opens the sample drawer and shows its tracking.
- **v27.773** — SAMPLING ▸ **Reports**: big metrics (products in progress, samples in progress), a type×season pivot of samples in progress, and a supplier KPI table (samples per request average, samples before approve/reject) with a detail popout + **PDF** download. Route `GET /api/product/reports/sampling`.
- **v27.774** — SAMPLING: sample tick **colour legend** key.
- **v27.775** — PRODUCTS filters slimmed (Season dropdown dropped — the tabs cover it — first tab is **All**); SAMPLING cell tidy (delivered shown as a compact badge on the SR line, DHL detail on hover).
- **v27.776** — SAMPLING grid: swatch column tracks the swatch size (`table-layout:auto`, ~88px for a 76px swatch vs 171px before); the reference fits on one row; column 3 shows a single colour (bulk, else development) with the component + category beneath.

### Sample lifecycle colour reference (for QA)
| State | Colour |
|---|---|
| Sample development | blue `#2361d8` (hollow) |
| Sample shipped / in transit | orange `#e8892b` (hollow) |
| Sample package received | light purple `#b79ce8` |
| Sample in review (= received tick) | dark purple `#6b3fbf` |
| Approved | green (`--pos`) |
| Rejected (new sample) | dark red `#c0271d` |
| Stop development | bright red `#ff2020` |
