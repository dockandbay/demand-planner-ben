# DEPLOY — HORIZON v26.862 → v26.907 (2026-08-12)

**Live now = v26.861** (Ben confirmed). This package = code **v26.862 → v26.907** + migrations **209, 211–219**. Branch `phase-2.1-suppliers`.

**Late additions (v26.906–907, code-only, no migration/env):** v26.906 demand-plan **variant image** http→https fix (mixed-content broken thumbnails; source URLs also cleaned on live) — n8n could re-introduce http on the next products sync, so consider upgrading the scheme in the Airtable/n8n image field; v26.907 demand-plan **YoY base** for target-recs + Summary/Targets uses the current month's full-month forecast instead of partial actuals. **Also a live DATA op already applied (no deploy action): `planner.subcategories.is_active=false` for "Poncho - Adults SEASONAL" + "Poncho - Kids SEASONAL"** (hidden from the demand plan) — set these in Airtable too or the next subcategories sync will re-activate them.

**No new npm deps.** New env vars = BLADE + Vercel KV (both optional / feature-flagged — see below). Files changed: `artifact_v16.7.html`, `server.mjs`, `supply/inject.html`.

---

## 1. Migrations to run on live (in order; all additive)
Migration 208 shipped in the v26.861 package. New since then:

| Mig | What | Note |
|---|---|---|
| `209_master_pos` | `purchase_orders.master_po` + `is_master` | Master-PO feature. **Run if not already applied.** |
| `211_products_3pl_onhand` | `products.inventory_<mkt>_3pl_onhand` | **Already applied to LIVE + data loaded (683 SKUs) via MCP — no action.** |
| `212_inventory_3pl_imports` | table: persist last 3PL import per market | Manage 3PL |
| `213_vpol_carton_from_products` | `v_purchase_order_lines` carton check → products-first | **Already applied to LIVE via MCP (SUG-0023 fix) — verify present.** |
| `214_supplier_timing_review` | table: exclude "our issue" + manual delay | Supplier Timing report |
| `215_v_sku_attrs` | view `v_sku_attrs` (carton/pallet products-first, else sku_labels) | see §5 — must exist before the code deploy |
| `216_inventory_fba_imports` | table: persist last FBA report per market | Manage FBA / FBA Aged |
| `217_v_sku_attrs_products_first` | view: flip mapped fields products-first | **superseded by 218** — running it is harmless, but 218 is the end state |
| `218_v_sku_attrs_products_only` | view: `v_sku_attrs` reads **FROM products only** (no sku_labels) | see §5 — **the end state** |
| `219_po_delivery_history` | table: PO delivery-date snapshots (delay detection) | SUG-0024 §5 |

Run order: 209, 212, 214, 216, 219 (tables) then 215 → 218 (views; 217 optional). 211/213 already live.

## 2. New env vars (all OPTIONAL — app runs without them)
| Var | For | Notes |
|---|---|---|
| `BLADE_PASSWORD` | EU 3PL import (bladepro.io API) | **secret.** Without it, Manage 3PL ▸ EU shows "set password"; US/UK/AU unaffected. `BLADE_USERNAME` defaults to the app account; `BLADE_WAREHOUSE_CODE` defaults `BOC`; `BLADE_STOCK_BODY` unused (built dynamically). |
| `KV_REST_API_URL` + `KV_REST_API_TOKEN` | Supabase egress/CPU fix (shared data cache) | Auto-set when a **Vercel KV** store is linked. **Without them the app is byte-identical to today** (in-process cache). See `deploy notes/KV_DATA_CACHE_2026-08-12.md`. |

`N8N_WEBHOOK_SECRET` already exists (now also gates `/api/data-cache/invalidate`).

## 3. New external services (both optional)
- **BLADE (bladepro.io)** — EU 3PL stock API. Read-only (login → PUT /stocks is a read). Only needs `BLADE_PASSWORD`.
- **Vercel KV (Upstash)** — shared data cache to stop every serverless cold start re-pulling ~12 MB from Supabase. **This is the fix for the egress/CPU quota overage.** Provision + link the store; the two env vars auto-populate.

## 4. n8n step (for the egress fix — do with the KV store)
After the node(s) that write `sales_actuals` / inventory into Supabase, add one HTTP call:
```
POST https://horizon.dockandbay.com/api/data-cache/invalidate
Header  x-webhook-secret: <N8N_WEBHOOK_SECRET>
```
Rebuilds the demand-data cache once + pushes to KV so the ~12 MB Supabase build runs per-upload instead of per-cold-start. (Forecast edits already self-invalidate.)

## 5. ⚠️ sku_labels decommission — ordering matters
The code now reads **`planner.v_sku_attrs`** everywhere (not `sku_labels` — 27 reads repointed). So **migration 215 (and ideally 218) MUST be applied to live before/with this code deploy**, or those reads fail. 218 makes the view read **products-only**; confirm live `planner.products` has the mapped columns populated by n8n (product_ean, carton_barcode, inner_barcode, barcode_*_name, colour_swatch_url, grs_material_carton, main_supplier_final, size/category/subcategory/status/size_short/variant_type/release_window, uk/us_carton_*). **Do NOT `DROP TABLE sku_labels` yet** — n8n still writes to it; drop only after the n8n write is retired (separate coordination).

## 6. Themes (v26.862 → v26.905)
- **Master PO** (consolidated supplier invoice): master is a real PO in the grid, children show with a `child` badge, remove-child / guarded delete, Create-master popup (mig 209). v862–872.
- **BUY & MOVE ▸ INVENTORY tab**: Manage 3PL (import US/UK/AU CSV + **EU via BLADE API**, persisted, ERP on-hand/available compare, gap filters, Cin7 Stocktake export, auto-load >2 days) + Manage FBA + **FBA Aged** (SKU/ASIN match, .txt/.csv, ignore amzn.gr., **monthly storage-cost estimate with editable rates** in `app_settings.fba_aged_rates`). migs 212/216.
- **SUG-0024 Inventory Status Report** (INVENTORY ▸ Status Report): Out of stock (3 lists 3PL/FBA/both, lost-sales units + £, inbound PO drawer links), Backorders, Low stock, Delivery vs forecast, **PO inbound delays + impacted products** (mig 219 + `/api/supply/po-delays`), In-production launches. `BP.statusMetrics` accessor.
- **Supabase egress/CPU fix**: KV shared data-cache (feature-flagged) + `/api/data-cache/invalidate`; 60 s polls → 4 min; PO-delivery snapshot throttled. See `SUPABASE_EGRESS_CPU_ANALYSIS.md`.
- Also: Supplier Timing exclusions (mig 214), Shipments ship-to/mode filters, SUG-0023 partial-carton fix (mig 213), Xero Compare column widths, PO grid status-first lazy load + NEEDS-ERP pill, Payments Report Paid-USD fix, nav spacing.

## 7. Data / no-action notes
- 211 (3PL on-hand) + its data already on live.
- No new files the server reads at boot (nothing to add to the Vercel `includeFiles` bundle this round).

Advance baseline to **v26.905** when Diviyaj confirms the pull.
