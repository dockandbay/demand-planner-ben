# DEPLOY 2026-08-06 — HORIZON v26.598 → v26.657

> **For Diviyaj.** Pull `phase-2.1-suppliers` @ **v26.657** and redeploy.
> **Baseline (last packaged):** v26.598 (see `DEPLOY_2026-08-05_v26.598.md`). This bundle = **v26.599 → v26.657**.
> ⚠️ If the previous **v26.591→598** bundle isn't live yet, this pull includes it too (it had **no** migrations/deps/env).

## TL;DR
- **Migrations:** **15 new — 183 → 197** (all additive: new tables + nullable columns; none alter existing data). Apply in order, only if not already on prod.
- **New npm deps:** **none.**
- **New env vars:** **none.**
- **New files the server reads at runtime:** **none.** (`HORIZON_PLM_REVIEW.md` is an internal strategy doc — not deployed/read.)
- **Buy engine:** **no buy-engine change in this bundle.** The DEMAND smoothing work (disregard-discontinued, auto-smooth, recalc-discontinued) is all **opt-in / on-demand** and does **not** touch the automatic forecast the buy plan reads — Buy 3PL baseline **24,332 unchanged** (verified in sandbox). Spot-check after deploy if you like, but no flag expected.
- **Cin7:** the new **DTC Mismatch import** and the existing FBA-transfer refresh call **production Cin7 read-only** (uses the existing `CIN7_AUTH`). No new Cin7 writes.

---

## Migrations (apply in order, only if not already on prod)

All additive — new tables or nullable columns; safe to run on live.

- **183_xero_compare_snapshot.sql** — shared Xero Compare snapshot (parsed XLSX rows persist 7 days, shared across users).
- **184_b2b_analysis.sql** — shared B2B allocation analyses (SCENARIO ▸ B2B "Save & share").
- **185_ka_carton_label_format.sql** — links a key-account client to a client-specific carton-label format (Paper Store labels).
- **186_klaviyo_bis.sql** — Klaviyo back-in-stock subscriber signal per (sku, market) (SUG-0018).
- **187_klaviyo_bis_uploads.sql** — per-market Klaviyo BIS upload metadata (file, who, when).
- **188_product_spec_types.sql** — managed list of spec-document types (SUG-0019 Specifications).
- **189_product_specs.sql** — product specification files (bytea), scoped All / Category / Size / SKU.
- **190_product_specs_confirm_suppliers.sql** — which suppliers a "Require Supplier Confirmation" spec is directed to (CSV, derived from scope).
- **191_product_specs_effective_split.sql** — "use from" split into two layers: effective_when (production|immediate) + effective_stock (useup|dispose).
- **192_zalando_stock_uploaded_by.sql** — track who uploaded the Zalando stock snapshot.
- **193_product_specs_approval_status.sql** — spec approval state: pending | approved.
- **194_product_specs_superseding.sql** — spec superseding (a new same-type/same-scope spec retires the prior → "past specification").
- **195_product_spec_approvals.sql** — per-supplier acknowledgement of a confirmation-required spec (one row per spec×supplier).
- **196_dtc_sales_orders.sql** — DTC Mismatch: `dtc_sales_orders` + `dtc_sales_order_lines` + `dtc_mismatch_review` (on-demand Cin7 SO import for DTC branches).
- **197_manufacturing_stock_cover.sql** — Manufacturing tab manual "covered by existing stock" entry, keyed (prod_no, component_sku).

---

## What's in the bundle (v26.599 → v26.657), by area

### PRODUCT ▸ Specifications — SUG-0019 (new feature) — migs 188–195
Spec-doc source-of-truth. P1 admin upload + scope (All/Category/Size/SKU) + "use from" two-layer rules (589→621); multi-file drag-drop upload, per-file type (619); supplier-confirmation targeting derived from scope (620); searchable Category/Size/Production pickers matching the PO-grid assign-supplier format (628/630); search + type pills, pending/approved status, list & matrix views (623); superseding + "Past specifications" history (624); **P3 supplier-portal display + approve + email + counter** (625); portal Specifications tab always visible (627).

### SUG-0020 DTC Mismatch — new BI & Reports report — mig 196
Reconciles open Direct-to-Client Cin7 **sales orders** against the **POs** mapped to them. On-demand Cin7 import (branches Direct-to-Client 5051 / UK B2B JLEW 27889 / UK B2B NEXT 27890), open = not-void AND not-dispatched. Import/reconcile endpoints (643), BI & Reports UI (644), red top-menu issue counter (645). **Import button hits production Cin7 read-only** — Ben triggers it.

### SUG-0018 Klaviyo BIS — new demand signal — migs 186/187
DEMAND ▸ Inputs ▸ Klaviyo BIS upload (accepts the combined workbook, auto-picks `<MKT>_BIS` tab) + per-country summary + plan 🔔 badge (611–617); **P2 enhanced-demand recommendation** feeding forecast uplift (647). Also a reusable market-colour palette + SHIP-TO badges (611).

### DEMAND smoothing suite (opt-in; buy plan unaffected)
- "Disregard discontinued from smoothing" per country/channel/sub-category (626); active vs discontinued unit split on the sub-category subtotal (629); fix so it keeps discontinued and boosts active only (631).
- Recalculate discontinued forecasts by velocity × seasonality, capped by stock (638); >48h staleness badge (639).
- **Auto-smooth** one-click sweep, per country/channel/sub-category (635) — reworked this session: flat one-row-per-subcategory layout (654), fires only when the gap is **under** the threshold (655), always re-caps discontinued to remaining stock first, Standard/Leader help, "Saved" indicator, sub-category left-aligned, help wording (656/657). Settings persist in `app_settings` (`smooth_disregard_disc`, `smooth_auto`), injected on load.
- **Undo** (last 10 forecast edits) (632).

### DEMAND ▸ Plan toolbar
Redesign + active-filter chips + filter panel (633/634/637); Release Window moved into **More Filters**, Undo + Download moved to the right of the plan controls row (640/652).

### SCENARIO ▸ B2B allocation — mig 184
Shared saved analyses + SKU picker + default 9wk cover (602); editable USD air-rush rate (604); layout, SKU dedupe, A4 PDF export (636).

### SUPPLY ▸ Manufacturing tab — mig 197
Existing-stock manual entry (per production × component SKU) added to coverage (651); **one pop-out drawer per BOM (parent) SKU** — production orders (qty + end dates) then each component's coverage, showing **only Manufacturing-branch POs** (653). Summary table + inline stock entry.

### CONFIG ▸ Supply chain ▸ Barcodes (new)
EAN CSV → barcode images; matched to the D&B example styling (full EAN-13 + human-readable digits); PNG default + size options (452/678/1000/1500/custom) + live preview (641/642/648). Plus per-supplier multi-supplier barcode files (already in the 591→598 note).

### Quality Control
Dates now dd-mmm-yy in both the admin list and the supplier portal; **supplier portal QC upload reworked** — 3-step (attach / specify type no-default / upload greyed until 1&2), dropdown-only batch, drag-drop, required prod/batch flash; **24h supplier delete** (649). Supplier portal background now white (650).

### PO grid / labels / Xero / Zalando
PO-grid multi-select **Hide** dropdown + exceptions filters (608–610); **Paper Store** client-specific carton labels (key-account-gated, 3-up A4, match template, blank inner qty) (601/603/605/606/607) — mig 185; Xero Compare + Zalando uploads shared server-side with 7-day persistence (599/600) — migs 183/192; Zalando upload attribution + "Analysis complete" badge (622).

---

## Post-deploy checks
1. Run migrations 183→197 in order.
2. PRODUCT ▸ Specifications loads + a spec's supplier-confirmation flow works in the portal.
3. BI & Reports ▸ DTC Mismatch: Ben clicks **Import** (prod Cin7 read); issues list + counter populate.
4. Manufacturing tab: "detail" drawer opens per BOM SKU; Existing-stock entry saves.
5. Spot-check a few SS27 buy SKUs (no buy-engine change expected — sanity only).
