# HORIZON deploy — 2026-08-10 · live v26.729 → **v26.826**

Package for Diviyaj. Live is at **v26.729**; this bundles everything up to **v26.826**.

Source: branch `phase-2.1-suppliers`. Changed files: `artifact_v16.7.html` (DEMAND/BUY SPA), `supply/inject.html` (SUPPLY/CONFIG/PRODUCT), `server.mjs`. Full per-version detail in `CHANGES.md`.

---

## 1. Migrations — run in order (planner schema)

Consolidated bundle: **`migrations/diviyaj_deploy_2026-08-10.sql`** (203→207 concatenated, idempotent / safe to re-run). Or run the individual files:

| # | File | What it does |
|---|------|--------------|
| 203 | `203_zalando_forecast.sql` | Seeds `planner.zalando_forecast` (Zalando EU forecast source — de-bakes the old baked ZAL data). Large data seed. |
| 204 | `204_sales_actuals_allow_zal.sql` | Widens the `sales_actuals` channel CHECK to allow `'ZAL'` (was DTC/B2B/FBA — blocked the Zalando LY-actuals upload). |
| 205 | `205_channels_registry.sql` | New `planner.channels` + `planner.countries` + `planner.channel_countries`, seeded to mirror today's 4 channels / 5 countries **exactly** (inert Phase-1 registry — no behaviour change). |
| 206 | `206_recently_received_pos.sql` | New `planner.recently_received_pos` feed (n8n-populated: `po` + `received_date`) for the auto-complete processor. |
| 207 | `207_product_dev_type.sql` | Adds `type` to `planner.product_dev_items` (default `'Product Development'`; existing rows backfilled). PRODUCT ▸ Type field. |

(Assumes live at v26.729 already has migrations ≤202. If `list_migrations` shows any of 196–202 missing, run those first.)

## 2. Env vars

- **`N8N_WEBHOOK_SECRET`** (new, optional) — when set, `POST /api/supply/received-pos/process` requires an `x-webhook-secret` header matching it (same env-gated pattern as `RESEND_API_KEY`). Unset → open (sandbox/dev). Set it on live and have n8n send the header.
- No other new env vars. `RESEND_API_KEY` (supply-planner emails) as before.

## 3. Dependencies

- **No new npm dependencies.** `npm ci` as usual.

## 4. n8n

- New workflow (or extend existing) to POST `planner.recently_received_pos` rows, then call `POST /api/supply/received-pos/process` (idempotent) → marks received POs COMPLETE + timeline note + supply-planner email.
- Zalando: forecast now lives in `planner.zalando_forecast` (mig 203) — the ETL should populate it going forward.

---

## 5. What's in this range (v26.730 → v26.826), grouped

**Trend Gaps (DEMAND ▸ Actions) — new + heavily iterated**
- New under-forecast **Trend Gaps** worklist: Category + SKU views, volume tiers (collapsible), country/channel filters (v764–772, 796).
- AI recommendation rework: single %, trend label, confidence band, direction-only "Mixed" (v757/760/763).
- Category **Apply**: writes a subcategory per-month override, uses the **same confidence engine as the plan cell "est"**, shares one month window with the list (current month included before the 15th), logs to the "R" rail (v807–811, 816).
- SKU-view apply: writes per-SKU override, logs to "R", persists immediately, repaints the plan (v820, 825).

**Zalando de-bake (Z1) — forecast now DB-sourced, feeds EU buy**
- Stages 2–4: forecast from Supabase (mig 203/204), EU **ZAL channel** in the demand plan, LY actuals shown, buy-3PL feed + live cascade, Summary inclusion (v745–758, 762). Removed baked ZAL globals.

**Modular channels** — Phase 1 registry (mig 205) + CONFIG ▸ Admin ▸ Channels (inert) (v750).

**Build / Create Purchase Orders (all-countries modal)**
- Full-page PO builder + multi-country Create-PO modal with MOQ flag; 3-step wizard; per-country ticks; inline qty; China-stock MOQ top-up; batch mandatory; MOQ value + hover; "Create Purchase Orders" button (v734–742, 797–801, 812–815).

**Deposits Drawdown report (new)** — SUPPLY ▸ Payments: per-production-ref burn-down, AU pooled, horizontal scroll, per-cell transactions popover, USD/GBP, per-column download, per-ref rebase, mobile-friendly (v779, 784–792).

**Payments** — completion due = today when awaiting invoice; completion due = ship date once invoiced; instant payment edits (no flash); final-invoice debounce (v767/768, 813, 815).

**PRODUCT ▸ PLAN** — new **Type** field (Product Development / Custom Order); Custom Order hides Season + uses `CUST-` ref prefix; grid Type filter (mig 207; v823/824). New product now lands on the item (v826).

**Master data missing** — relocated red "A" to the Actions count + MASTER DATA badge; per-field "A" markers; counts on the ACTIONS badge (v767, 821, 822).

**Summary / Edit targets** — 3-tab (Summary / Edit targets / Target recommendations); fixed-budget market total with implied+rebalance subcategories, conflict ⚠, grouping, sticky header (v731–733, 798–806).

**Key Accounts (DEMAND ▸ Inputs)** — grid tidy-up + import/paste templates + R channel fix (v781–783).

**Cin7 / 3PL invoicing** — resumable + logged imports (no serverless timeout / false failure); error handling; editable account map (v776, 778).

**Received POs** — auto-complete + timeline + email + ✉ bell surfacing; shared-secret gate (mig 206, `N8N_WEBHOOK_SECRET`; v753–756).

**Quality Control** — full-width fixed-layout table, column widths, dd-mmm-yy (v817–819).

**Price changes (Inputs)** — replaces the Revenue tab; left-aligned; month picker (v733, 816, 817).

**Performance / caching** — SUPPLY read caches (ORDER PLAN default view), SKU-data lazy-load (`?lazysku=1`, ~46% smaller mobile page), cross-instance cache coherence (fixes stale Cash Flow after edits) (v730, 740, 769).

**Nav / cache UX** — SUPPLY nav respects the clicked section (no flash to Actions); "↻ Refresh cache" buttons (SUPPLY nav + BUY plan) (v818, 820).

---

## 6. Post-deploy smoke test
- DEMAND ▸ Actions ▸ Trend Gaps loads; Category/SKU Apply writes + persists.
- SUPPLY ▸ Payments ▸ Deposits Drawdown renders (per-production columns).
- PRODUCT ▸ PLAN Type filter defaults to Product Development; create a Custom Order → `CUST-` ref.
- EU demand plan shows the ZAL channel; a ZAL forecast feeds EU Buy 3PL.
- `POST /api/supply/received-pos/process` with the webhook secret set.
