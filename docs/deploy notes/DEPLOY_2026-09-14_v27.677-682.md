# Deploy note — v27.677 → v27.682 (for Diviyaj)

**Date:** 2026-09-14
**Branch:** `phase-2.1-suppliers` (pushed)
**Prev package:** v27.662–676 (live 11-Sep)

---

## Migrations to run on live
- **`272_key_account_client_fba_fields.sql`** — adds `consignee`, `contact_person`, `contact_number`, `freight_forwarder` (text, nullable) to `planner.key_accounts`. Additive, safe. (Applied to sandbox already.)

No other migrations. **No new env vars.**

---

## What's in the package

- **v27.677 — REVERT the v27.666 header-based auth bypass (security).** Reverted the `PLANNER_KEY` gate to cookie/key-only (pre-v27.666). Your prod fix (signed login cookie passes the gate; headers never do) stays the source of truth. **Still TODO on Ben's side:** mirror your cookie-only gate here once you send the cookie name + verification, so the next port merges cleanly. server.mjs only.
- **v27.678 — Payments report: deposit Xero code inherits from linked production.** A deposit row with a blank `xero_account_code` now falls back to the linked production's code (`prod_numbers` by `prod_no`, leading `P` normalised) — same as PO lines. AU deposits still force `620.00 AU`; a deposit's own code still wins. server.mjs only.
- **v27.679 — Key accounts hold the Client/FBA delivery-contact fields (needs mig 272).** The SUG-0041 fields (Consignee, Contact person/number, Freight forwarder) now also live on the key account and default onto a PO's Client/FBA tab when the KA is applied. server.mjs + inject.html.
- **v27.680 — AU deposit payments route to `620.00 AU` when funded from an AU deposit.** Payments-report PO-leg now forces `620.00 AU` when the linked deposit is an AU deposit (`deposits.country='AU'`), in addition to the PO's own country. Catches AU orders whose `country_code` arrives as `OT`/blank (AU Direct-to-Client). **Byte-identical on current live data** — forward safety net only. server.mjs only.
- **v27.681 — B2B scenario: date- & market-aware expedite feasibility.** `/api/scenario/b2b` production sub-select now returns `po_market`, `exped_weeks`, `exped_land`; the client counts an in-production PO toward cover only if it's in the order's market AND can be expedited to land by the required-by date (today + supplier `expedited_production_weeks` + market air lead). Late / cross-market production is shown as an option, not counted. server.mjs + inject.html.
- **v27.682 — Sales Planning scenario: green multi-SKU filter box.** Client-only filter box on `#/demand/scenario/salesplan`; flows through count + CSV export. inject.html only.

---

## Live-data changes this period (FYI — already applied; not part of the code deploy)
These were direct live writes (Ben-authorised); flagged so they don't surprise a reconcile:
- **Weireken → Weierken** supplier rename across live structured fields (Airtable + Cin7 sources also renamed by Ben, so it's durable).
- **FENW28480-7 / -8** `tpl_cin7_orders.cost_center` set to `COGS - UK - Wholesale` (Cin7 also updated by Ben) — was blank, broke the ILG reclass journal account code.
- **`prod_numbers` orphan row `P58` deleted** (nothing linked; the P58 deposits inherit code `58` = `620.38 P58`).
- **PO-1697839** `country_code` corrected to `AU` by Ben (was `OT`, so its AU deposit payment had leaked to `620.36 P56`).

## Open follow-ups (no action needed to deploy)
- **AU deposit rule (v27.680):** need to confirm whether live Xero posting comes from this report (then v27.680 is the source fix) or an n8n workflow (then mirror the same rule there). Two data-fix POs remain: **PO-49AUHD1** (country `UK`, branch AU Coghlans → correct its country) and **PO-44AUXR1** (AU ref but UK deposit — left as-is by design).
- **Auth cookie:** send the signed-cookie name + verification so v27.677 can be replaced with the real cookie-only gate here.
