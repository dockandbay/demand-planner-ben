# Deploy note for Diviyaj — 2026-07-30

**Live baseline:** v26.272 (confirmed shipped to production — "✅ Shipped to production — Horizon v26.272"; migrations up to **158** applied).
**This package:** v26.273 → **v26.299**.

Pull `phase-2.1-suppliers` (HEAD = v26.299) into the production repo, wire to prod data, deploy.

---

## Migrations to apply (production Supabase `oolwklahstnvocaugryg`)

Three new migrations since v26.272 — all additive, all defensive in code (the app works before they're applied):

- **`migrations/159_email_log.sql`** — new `planner.email_log` table (CONFIG ▸ Email log). Defensive: without it, logging is a no-op (never blocks a send) and the panel is empty.
- **`migrations/160_quality_docs.sql`** — new `planner.quality_docs` table (SUPPLY ▸ Quality Control; admin + supplier-portal document uploads, stored as bytea). Defensive: endpoints return empty without it.
- **`migrations/161_supplier_expedited_weeks.sql`** — adds `planner.suppliers.expedited_production_weeks` (default 6; XR Textile / Lixin / Jinma = 3). Read defensively (defaults to 6 until applied).

**Migrations 157 (`note_mentions`) and 158 (`po_change_log`) were part of the v26.272 package and should already be live** — no need to re-apply.

No other schema changes.

## New env vars / dependencies
- **None.** (The Email log + stakeholder/mention emails use the existing `RESEND_API_KEY` — already configured in prod; no send happens and logging is skipped when it's absent.)

## Files changed (v26.273 → v26.296)
- `server.mjs`
- `supply/inject.html`
- `supply/portal-view.js`
- `artifact_v16.7.html` (DEMAND version badge)
- (`CHANGES.md`, `package.json` — version log only)

---

## What's in this package (v26.273 → v26.299)

**Admin / CONFIG**
- **CONFIG ▸ Email log** (admin-only): logs every Resend send; sent-per-month counts + date range (default 7 days) + search — **needs migration 159**. (v26.273)

**Quality Control (SUG-0001)**
- New top-level **SUPPLY ▸ Quality Control** + supplier-portal **Quality Control** tab: test reports / GRS transaction & scope certs / inspection & compliance docs, stored in-DB, mapped to prod/batch/PO, admin + supplier upload — **needs migration 160**. (v26.274)
- QC docs also surface on the PO **Master Data** & **Documents** tabs (shared across the PO's production/batch for that supplier). (v26.275)
- Portal QC: prod# / batch# / PO# are searchable dropdowns from the supplier's PO history (prod+batch sorted descending); free-text when no history. (v26.289)

**Payments / Cash Flow**
- **PAYMENTS ▸ By Supplier** (SUG-0006): CSV + copy-to-clipboard export, defaults to the latest production, active filters shown in black. (v26.286)
- Cash Flow report: PO / shipment refs open the drawer (not a full page). (v26.276)
- Cash Flow: likely-pay-date calendar picker wired (was dead); post-save stays in PAYMENTS. (v26.279)
- PAYMENTS ▸ Deposits: add / save / production-change now refresh in place instead of bouncing to Productions. (v26.277)

**Navigation / UX**
- Main SUPPLY menu tabs are real anchors — Cmd/Ctrl/middle-click opens a section in a new tab. (v26.278)
- Fixed a persistent fast-tip that stuck when a drawer opened over a hovered ref (any mousedown now dismisses it). (v26.280)
- **DEMAND version badge** now renders as the amber HORIZON badge (matches SUPPLY) instead of faint grey text. (v26.293)
- **Level-2 menu alignment**: removed the leading "REPORTS" label and normalised `#report-tabs`; CONFIG & PRODUCT level-2 menus converted from pill style to the underline-tab style used by the other views. (v26.294)
- **REPORTS menu + version badge** now match the other views: sub-tabs switched from the level-1 `.view-toggle` style to `.dnav`, and the amber version badge is relocated onto the report bar. (v26.298)
- **CONFIG menu + version badge**: CONFIG level-2 nav now shows the amber HORIZON badge (it had none) and its tabs use the grey→dark `.stab` colours (was blue). (v26.299)

**Barcodes**
- PURCHASE ORDERS ▸ Barcodes: new **⤓ All (product + carton + inner)** button — downloads all three barcode kinds in view as one ZIP (foldered by supplier, "<supplier> - Barcodes"), honouring the active filters + A4/PNG setting. (v26.297)
- Portal carton-barcode download now includes inners (SUG-0002). (v26.284)
- Barcode downloads: hover-to-cancel while rendering. (v26.281)
- Barcode ZIP honours the supplier filter (no more fanning multi-supplier SKUs into other suppliers' folders). (v26.282)
- Barcode ZIP filename reflects supplier + batch/prod (e.g. `carton_inner_barcodes_A4_by_Lixin_57.0626.zip`). (v26.283)

**Suggestion box**
- Stakeholder emails on new suggestion + status change (SUG-0003, fixes the missing new-suggestion email); add-another without refresh (SUG-0004). (v26.285)
- Guard unsaved input: confirm on Cancel/✕ + don't dismiss on backdrop click while typing (SUG-0010). (v26.291)

**Supplier portal — samples & shipments**
- **Link a sample to MULTIPLE shipments** (SUG-0005 part B): "Link to shipment" search box (existing shipments + "＋ New shipment" in the dropdown), per-shipment unlink; server modes `link`/`unlink` on `/api/portal/product-sample/:id/assign` (legacy single-assign kept). (v26.290)
- Shipments shown in a dedicated **📦 Shipments** box (right column on desktop); "＋ New shipment" stays on the product page and refreshes in place. (v26.293)
- Sample-shipment **contents refresh** on expand (fixes stale cache where dev samples linked from PRODUCT ▸ samples didn't show). (v26.295)
- Portal menu heading **"Samples" → "Sample shipments"**. (v26.295)
- Tracking code no longer duplicated — the link now reads **"track here ↗"**. (v26.296)

**BI & Reports**
- New **SUPPLY ▸ BI & Reports ▸ PRODUCTION SUMMARY** report (SUG-0009): total qty in production per category (collapsed, tap to expand to SKUs); PROD# + BATCH (descending) + SUPPLIER filters; nothing renders until a prod#/batch is chosen; Categories CSV + SKUs CSV exports; mobile-optimised. New endpoint `GET /api/supply/bi/production-summary`. (v26.292)

**Suppliers / config**
- Supplier **Expedited production weeks** field in CONFIG ▸ Suppliers (SUG-0008 part 1) — **needs migration 161**. (v26.287)

---

## Needs your action beyond the deploy (not code in this package)

- **SUG-0007 (session re-validation / 7-day max login):** app-side heartbeat is built (v26.288) but only *does* anything once the **auth proxy** is configured (7-day session + ~4h Google re-validation + a logout URL). Full spec + ownership split is in **`SUG-0007_session-reauth-spec.md`**. Set `planner.app_settings.auth_logout_url` to the proxy logout URL when ready.

## Still open on Ben's side (no action for you)
- **SUG-0008 part 2** — the BI calc wiring (urgent-buy / expedited-production use `expedited_production_weeks`) is not yet done; awaiting Ben's confirm on the formula.
