# DEPLOY — HORIZON v26.968 → v27.6.34 (2026-08-14)

**Package** = `v26.968 → v27.6.34`, branch `phase-2.1-suppliers`.
**Follows** the `DEPLOY_2026-08-13.md` package (v26.862 → v26.967). If that one isn't live yet, apply it first (this package stacks on it).

Full itemised change log: `CHANGES.md` (v26.968 → v27.6.34, ~80 entries). This note covers **migrations, env, test status, and risk flags** — read those before promoting.

---

## 1. Migrations to run on live (in order)
All in `migrations/`. None destructive; the scope trigger (230) redefines a function + backfills.

| # | File | What it does |
|---|------|--------------|
| 224 | `224_sales_actuals_allow_tik.sql` | Allow channel `TIK` (TikTok) in `sales_actuals`. |
| 225 | `225_sample_notify_emails.sql` | `sample_requests.notify_emails` (stakeholder emails, "email when shipped"). |
| 226 | `226_action_metrics_snapshot.sql` | Weekly open-actions scoreboard table (SUPPLY ▸ Reports ▸ Metrics). |
| 227 | `227_action_metrics_reallocations.sql` | Adds `reallocations` count to that scoreboard. |
| 228 | `228_sample_purpose_accounts.sql` | Config table: sample purpose/type → Xero account code. |
| 229 | `229_set_bom.sql` | **SETS.** `planner.set_bom (output_sku,input_sku,input_quantity)` — Build-on-Fly-Sets BOM. |
| 230 | `230_planning_scope_include_sets.sql` | **SETS.** Extends the mig-109 scope trigger to `variant_type IN ('MASTER','SET')` so sets enter planning scope. Includes a one-time backfill. |

*(Migration 223 `ship_plan_locks` was in the 08-13 package.)*

## 2. Env vars
**None new** in this package. (The KV store + `RESEND_API_KEY` prerequisites from the 08-13 note still apply if not already done.)

## 3. Standalone `vercel.json`
No change from the 08-13 package (still `maxDuration: 300`). Confirm it doesn't revert.

---

## 4. What's in this package (grouped)

**SUPPLY — Shipments / Plan Shipments** *(Ben-approved)*
- Orphans = **empty shipment records** (no POs aboard, excl. completed); "Clean up orphans" **deletes** them (POs untouched). Orphan self-shipments now show in the grid.
- **Create POs → Order Plan visible immediately** (server cache invalidation on PO write).
- Plan Shipments: **top-up recommendations merged one card per container** + single Apply; **"Keep Lixin & XR Textile apart"** toggle.
- Admin false "Read-only" on demand/supply/config writes fixed (admin bypass).

**Samples (admin + portal)** *(mostly Ben-approved)*
- **New sample appears in the portal immediately** (cache invalidation).
- **PO-like status lifecycle: FUTURE → PRODUCTION → COMPLETED (+ CANCELLED)**; **FUTURE is hidden from the supplier portal** (a sample only reaches the supplier at PRODUCTION); grid pills OPEN / COMPLETED / ALL; FUTURE colour-coded light-purple (matches PO grid).
- New-sample form: searchable SKU picker (qty beside tick, coloured Add), open + toast on create, dev→product link; **unique "Find past address"**.
- Timeline: SKU/qty changes show the exact diff ("deleted X x1, added Y x2"); supplier "Confirmed" posts a shared note.
- Xero: sample-charge split by purpose → account code (mig 228).

**PRODUCT**
- Samples tab lists **linked sample requests (SR-nn)** + "+ Add to new sample request" (Samples tab & MASTER DATA). Cell edits flash green on save.

**DEMAND plan**
- More Filters reworked (Tier → Release → From-replacement → inline category checklist, taller/wider). Display Settings clamped to viewport. Auto-smooth un-smoothed-summary + CSV.

**CONFIG ▸ BOM**
- "BOM" menu = **Manufacturing** + **Build on Fly Sets** sub-tabs (mig 229 + endpoints).

**SETS feature (PARTIAL — see §6)**
- Sets in planning scope (mig 230), excluded from the buy plan, 0% subcategory contribution, SET badge + 3PL-only inventory, show/hide/only filter, `SET_BOM` injected.

**Other (pre-session tail v26.968→v27.6.3):** TikTok (TIK) channel (mig 224), samples email-stakeholders (mig 225), open-actions scoreboard (migs 226/227), DTC-mismatch UI fixes, Plan Shipments lock, etc. — see CHANGES.md.

---

## 5. Test sign-off status (Ben, 2026-08-14)
**Approved:** Shipments/Plan-Shipments (orphans, create-PO immediacy, merged top-ups, keep-apart); Samples portal-immediacy, status lifecycle + pills, new-sample form; CONFIG BOM; SETS demand-side.
**Not yet signed off:** PRODUCT samples-tab + cell-flash; DEMAND More-Filters / Display-Settings / Sets-filter (built + syntax-clean, awaiting Ben's browser verify).
**Known small follow-ups (not built):** dev badge + product link on the sample *detail* lines; "Find past address" on the sample *detail*.

## 6. ⚠️ Risk flags — read before promoting
1. **SETS is PARTIALLY built.** Done: sets show in DEMAND (scope via mig 230), **excluded from buying** (buy overlay drops `vt==='SET'`), 0% contribution. **Not done: the P2 component explosion** (set demand → component buys) — so sets currently carry **0 demand/forecast** and never buy. It's safe to deploy (masters provably unchanged; no set buy lines) but the feature is **not complete** — sets won't drive any buying yet.
2. **Buy-plan snapshot NOT yet run for the SETS scope change.** Safe by construction (buy universe = non-sets, unchanged; sets excluded), but a formal `BP.buyplanItems` before/after (mig 230 on vs off) is the recommended gate. If you want, hold migs 229/230 + the SETS commits and ship the rest.
3. **Sample status change alters portal visibility:** new samples default **FUTURE and are hidden from the supplier portal** until moved to PRODUCTION. Existing live samples: run the sandbox-equivalent backfill if you want legacy 'open'/'planned' → PRODUCTION (I did this in sandbox; on live the trigger/default handles new ones, but existing rows keep their stored status).
4. **Availability data:** `temp/update_sku_availability.sql` (from Ben's CSV) was checked against live and **already matched** — no action needed.

---

## 7. TL;DR for Diviyaj
Run migrations **224 → 230** in order. No new env vars. Branch `phase-2.1-suppliers`. **Decision:** ship SETS (partial-but-safe) now, or hold migs 229/230 + SETS commits until the P2 explosion + buy-snapshot are done — everything else is independent and Ben-approved.
