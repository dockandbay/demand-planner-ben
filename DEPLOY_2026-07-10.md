# Deploy: HORIZON v25.360 → v25.378 (2026-07-10)

**Baseline:** live = **v25.360** (Diviyaj, verified 2026-07-09; migrations 093–102 applied).
Branch: `phase-2.1-suppliers`. Supersedes `DEPLOY_2026-07-09.md` (that targeted v25.325 + migs 101/102,
both now live).

---

## 1. Migrations to run on live — 5 (in order)

| # | File | What |
|---|---|---|
| 103 | `migrations/103_app_permissions.sql` | Creates `planner.app_permissions` + seeds **12 users**: ben/diviyaj/sarah/andy/abi = admin+full; hannah/avry/liam/alexandria/madison/gabby = SUPPLY-only; zera = SUPPLY+DEMAND. Idempotent. |
| 104 | `migrations/104_product_invoice_fields.sql` | Adds `products.sku_invoice_title` + `hscode_uk/us/eu/ca/au`; **987-row UPDATE** from Ben's CSV (matched by SKU). Idempotent. |
| 105 | `migrations/105_invoice_consignees.sql` | Creates `planner.invoice_consignees` + seeds UK/US/EU/AU consignee/notify addresses. |
| 106 | `migrations/106_supplier_te_id.sql` | Adds `suppliers.te_id`; seeds Lixin = TE-00055808. |
| 107 | `migrations/107_preship_not_required.sql` | Adds `purchase_orders.preship_not_required` (boolean). |

All additive + idempotent (`ADD COLUMN IF NOT EXISTS` / seeded upserts). Safe to run once.

## 2. New dependency — **exceljs**

`package.json` now lists `"exceljs": "^4.4.0"`. Run **`npm install`** on the deploy (pure-JS, Vercel-safe) —
the invoice generator imports it. (`asnpdf.mjs` is dependency-free.)

## 3. New file to include — the invoice template shell

`templates/invoice-packing-template.xlsx` (committed) is read at runtime by `invoice.mjs`. Ensure it's
present in the deployed bundle (it's in the repo, not gitignored).

## 4. Env vars — none new

`CIN7_AUTH` already wired. Permissions enforcement keys off the auth-proxy email header the live gate
already forwards (`authUser()` in server.mjs); no config change.

## 5. New source files

`invoice.mjs`, `asnpdf.mjs` (both committed). Rest is edits to `server.mjs`, `supply/inject.html`,
`supply/portal-view.js`, `artifact_v16.7.html`.

## 6. What changed (v25.361 → v25.378)

- **Permissions / access control** (103 + v25.361–363): CONFIG promoted to a **top-level** view; email-based
  **edit gating** (SUPPLY/DEMAND) enforced server-side, **live-only** (no auth email = read-only by default);
  admin-only Permissions panel; read-only toast/banner. FBA override + SCENARIO always editable; supplier
  portal unaffected.
- **Invoice + Packing List generator** (104/105/106 + exceljs + template; v25.365–366, 369–371, 373):
  Commercial (PO) / Tax (shipment) xlsx from the template shell + live data; CONFIG ▸ Consignees; TE-ID header;
  download buttons on 4 surfaces (supply PO payments panel + shipment drawer, portal PO + shipment).
- **Cin7 import** (v25.367/368): imported POs land as **PRODUCTION** (visible) + grid auto-filters to the PO.
- **Pre-shipment documents** (107 + v25.372–377): SHIPMENTS sub-tab per PO — AU Coghlans **ASN + pallet-label
  PDF**, FBA/AWD **carton+pallet barcode uploads**, EU iFulfillment **IDN-label uploads**; overdue exception
  (SHIPMENTS badge + PO-ref grid badge + ACTION ITEMS filter), scoped to PRODUCTION/ready-to-ship, with a
  **"Not required"** tick; supplier-portal downloads.
- **Fixes / UX**: FBA Cartons filter "Any" default + pill active-state fix (v25.364/378); PROD/BATCH filter
  dropdowns most-recent-first, PROD ACTIVE-only; shipment-drawer Flexport link.

## 7. Post-deploy notes for the n8n / Airtable sync

These columns are **one-off seeds**; teach the product/supplier sync to **preserve or populate** them so a
full-row replace doesn't wipe them, and new records aren't NULL:
`products.sku_invoice_title`, `products.hscode_*`, `suppliers.te_id`, `suppliers.cin7_member_id`.

## 8. Known gaps (non-blocking)

- 138 products have no `sku_invoice_title`/HS yet; `te_id` only Lixin; 7 suppliers' `cin7_member_id` NULL —
  Ben to top up in Airtable.
- Pre-shipment "NEW …" unread timeline counter not built (downloads work regardless).
- Product sync scope (ACTIVE vs PLANNED) still pending Ben's value — feeds the PROD dropdown + invoice fields.
